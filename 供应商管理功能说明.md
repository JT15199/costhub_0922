# CostHub 供应商管理功能说明 v2.3.2

## 功能概述

CostHub v2.3.2 新增了完整的供应商管理体系，包括**器件供应商管理**和**整机供应商管理**，适用于ODM/OEM业务场景，支持多供应商报价对比、份额分配、加权成本计算和完整的成本变动追溯。

---

## 一、器件供应商管理

### 适用场景
- 同一器件有多家供应商报价
- 需要根据采购份额计算加权成本
- 追踪供应商价格变动历史
- 器件成本变化影响项目成本的追溯

### 核心功能

#### 1. 多供应商报价管理
- 每个器件可添加**多个供应商**
- 记录供应商名称、报价、份额比例、启用状态、备注
- **简化设计**：只保留核心字段，移除了供应商代码、交期、MOQ等非必要字段

#### 2. 份额比例与加权成本
**公式**：`加权成本 = Σ(启用供应商价格 × 份额比例) / Σ(启用供应商份额比例)`

**示例1：两家供应商**
- 供应商A：¥10.00，份额60%
- 供应商B：¥12.00，份额40%
- **加权成本** = 10 × 0.6 + 12 × 0.4 = **¥10.80**

**示例2：份额归一化**
- 供应商A：¥10.00，份额30%
- 供应商B：¥12.00，份额20%
- 系统自动归一化为 60% : 40%
- **加权成本** = 10 × 0.6 + 12 × 0.4 = **¥10.80**

**停用供应商**：不参与加权成本计算

#### 3. 价格变动强制记录
- 修改供应商价格时，系统**强制弹窗**要求输入变动原因
- 必须填写原因才能保存（不允许空白提交）
- 自动记录到 `part_supplier_price_history` 表
- 同时记录到 `cost_change_log` 表，追踪影响范围

#### 4. 自动更新器件成本
当供应商价格变化时，系统自动执行：
1. 重新计算器件加权成本
2. 更新 `parts.cost` 字段
3. 记录成本变动日志（`cost_change_log`）
4. 查询使用该器件的所有项目
5. 标记受影响的项目列表（JSON格式存储）

#### 5. 价格历史追踪
- 点击供应商卡片的"时钟"图标查看历史
- 显示：旧价格、新价格、变动金额、时间、变动原因
- 支持追溯任意时间点的价格变化

### 使用流程

1. **打开供应商管理**
   - 器件库页面 → 点击器件行的"管理"按钮 → 右侧抽屉打开

2. **添加供应商**
   - 点击"添加供应商" → 填写表单 → 点击"添加"
   - 必填项：供应商名称、价格、份额比例
   - 选填项：状态（启用/停用）、备注

3. **修改价格**
   - 点击供应商卡片的"编辑"图标
   - 修改价格后点击"更新"
   - 系统弹窗要求输入变动原因
   - 填写原因后确认，价格变动生效

4. **查看加权成本**
   - 抽屉顶部统计卡片实时显示
   - "加权成本"：根据份额自动计算
   - "供应商数量"：当前器件的供应商总数

5. **查看价格历史**
   - 点击供应商卡片的"时钟"图标
   - 查看完整的价格变动记录

---

## 二、整机供应商管理（ODM项目）

### 适用场景
- ODM项目有多家工厂报价
- 需要对比不同工厂的整机报价
- 根据订单分配份额计算加权报价
- 追踪工厂报价变动历史

### 核心功能

#### 1. 整机报价管理
- 每个项目可添加**多个整机供应商**
- 记录供应商名称、整机报价、份额比例、启用状态、备注
- 适用于ODM模式，工厂提供整机报价

#### 2. 加权报价计算
**公式**：`加权报价 = Σ(启用供应商报价 × 份额比例) / Σ(启用供应商份额比例)`

**示例：三家ODM工厂**
- 富士康：¥500.00，份额50%
- 和硕：¥520.00，份额30%
- 广达：¥510.00，份额20%
- **加权报价** = 500 × 0.5 + 520 × 0.3 + 510 × 0.2 = **¥508.00**

#### 3. 报价变动记录
- 修改整机供应商报价时，系统**强制弹窗**要求输入变动原因
- 自动记录到 `project_supplier_price_history` 表
- 同时记录到 `cost_change_log` 表

#### 4. 报价历史追踪
- 点击供应商卡片的"时钟"图标查看历史
- 显示：旧报价、新报价、变动金额、时间、变动原因

### 使用流程

1. **打开整机供应商管理**
   - 项目管理页面 → 选择项目 → 点击"🏭 整机供应商"标签页

2. **添加供应商**
   - 点击"添加供应商" → 填写表单 → 点击"添加"
   - 必填项：供应商名称、整机报价、份额比例
   - 选填项：状态（启用/停用）、备注

3. **修改报价**
   - 点击供应商卡片的"编辑"图标
   - 修改报价后点击"更新"
   - 系统弹窗要求输入变动原因
   - 填写原因后确认，报价变动生效

4. **查看加权报价**
   - 标签页顶部统计卡片实时显示
   - "加权报价"：根据份额自动计算
   - "供应商数量"：当前项目的供应商总数

---

## 三、成本变动追溯系统

### 功能概述
完整记录成本变动链路，从供应商价格变动到项目成本变化的全流程追溯。

### 变动链路

#### 1. 器件供应商价格变动
**触发**：修改器件供应商价格  
**记录**：`cost_change_log` (change_type: 'part_supplier_price')  
**影响**：器件加权成本变化

#### 2. 器件成本变动
**触发**：器件加权成本重新计算  
**记录**：`cost_change_log` (change_type: 'part_cost')  
**影响**：使用该器件的所有项目BOM成本

#### 3. 项目BOM成本变动
**触发**：器件成本变化影响项目  
**记录**：`cost_change_log` (change_type: 'project_bom_cost')  
**影响范围**：JSON格式记录受影响的项目ID列表

#### 4. 整机供应商报价变动
**触发**：修改项目整机供应商报价  
**记录**：`cost_change_log` (change_type: 'project_supplier_price')  
**影响**：项目整机加权报价

### 日志字段说明

```typescript
interface CostChangeLog {
  id: number;
  change_type: string;      // 变动类型
  ref_type: string;         // 关联类型（part/project/part_supplier/project_supplier）
  ref_id: number;           // 关联ID
  ref_name: string;         // 器件名称或项目名称
  supplier_name: string;    // 供应商名称
  old_value: number;        // 旧值
  new_value: number;        // 新值
  change_reason: string;    // 变动原因
  impact_scope: string;     // JSON格式：影响范围
  changed_at: string;       // 变动时间
}
```

### 使用场景

**场景1：追溯器件成本变化的根源**
1. 查询 `cost_change_log` 表，筛选 `ref_type = 'part'` 且 `ref_id = 器件ID`
2. 查看 `change_type = 'part_supplier_price'` 的记录
3. 获取供应商名称、价格变动、变动原因

**场景2：查询项目成本受哪些器件影响**
1. 查询 `cost_change_log` 表，筛选 `change_type = 'project_bom_cost'`
2. 解析 `impact_scope` 字段，找到包含目标项目ID的记录
3. 查看器件名称、成本变动、变动原因

**场景3：追踪整机报价变动**
1. 查询 `cost_change_log` 表，筛选 `ref_type = 'project_supplier'` 且 `ref_id = 供应商ID`
2. 查看 `change_type = 'project_supplier_price'` 的记录
3. 获取报价变动、变动原因、时间

---

## 四、数据库结构

### 表结构

#### 1. part_suppliers（器件供应商表）
```sql
CREATE TABLE part_suppliers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  part_id INTEGER NOT NULL,
  supplier_name TEXT NOT NULL,
  price REAL NOT NULL DEFAULT 0,
  share_ratio REAL NOT NULL DEFAULT 100,
  is_active INTEGER DEFAULT 1,
  remark TEXT DEFAULT '',
  created_at TEXT DEFAULT (datetime('now','localtime')),
  updated_at TEXT DEFAULT (datetime('now','localtime')),
  FOREIGN KEY (part_id) REFERENCES parts(id) ON DELETE CASCADE
);
```

#### 2. part_supplier_price_history（供应商价格历史表）
```sql
CREATE TABLE part_supplier_price_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  supplier_id INTEGER NOT NULL,
  old_price REAL NOT NULL,
  new_price REAL NOT NULL,
  changed_at TEXT DEFAULT (datetime('now','localtime')),
  change_reason TEXT DEFAULT '',
  FOREIGN KEY (supplier_id) REFERENCES part_suppliers(id) ON DELETE CASCADE
);
```

#### 3. project_suppliers（整机供应商表）
```sql
CREATE TABLE project_suppliers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id INTEGER NOT NULL,
  supplier_name TEXT NOT NULL,
  quoted_price REAL NOT NULL DEFAULT 0,
  share_ratio REAL NOT NULL DEFAULT 100,
  is_active INTEGER DEFAULT 1,
  remark TEXT DEFAULT '',
  created_at TEXT DEFAULT (datetime('now','localtime')),
  updated_at TEXT DEFAULT (datetime('now','localtime')),
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);
```

#### 4. project_supplier_price_history（整机供应商价格历史表）
```sql
CREATE TABLE project_supplier_price_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  supplier_id INTEGER NOT NULL,
  old_price REAL NOT NULL,
  new_price REAL NOT NULL,
  changed_at TEXT DEFAULT (datetime('now','localtime')),
  change_reason TEXT DEFAULT '',
  FOREIGN KEY (supplier_id) REFERENCES project_suppliers(id) ON DELETE CASCADE
);
```

#### 5. cost_change_log（成本变动日志表）
```sql
CREATE TABLE cost_change_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  change_type TEXT NOT NULL,
  ref_type TEXT NOT NULL,
  ref_id INTEGER NOT NULL,
  ref_name TEXT NOT NULL,
  supplier_name TEXT DEFAULT '',
  old_value REAL NOT NULL,
  new_value REAL NOT NULL,
  change_reason TEXT DEFAULT '',
  impact_scope TEXT DEFAULT '{}',
  changed_at TEXT DEFAULT (datetime('now','localtime'))
);
```

---

## 五、典型应用案例

### 案例1：多供应商器件成本管理

**背景**：某显示器项目使用的LCD面板有3家供应商报价。

**操作步骤**：
1. 在器件库中找到"LCD面板"器件
2. 点击"管理"按钮，添加3家供应商：
   - 京东方：¥150.00，份额50%
   - 华星光电：¥155.00，份额30%
   - 群创光电：¥152.00，份额20%
3. 系统自动计算加权成本：150×0.5 + 155×0.3 + 152×0.2 = **¥151.40**
4. 器件库中该器件的成本自动更新为¥151.40
5. 使用该器件的所有项目BOM成本自动重新计算

**后续变动**：
- 京东方报价降至¥148.00
- 系统弹窗要求输入原因："原材料价格下降"
- 加权成本更新为：148×0.5 + 155×0.3 + 152×0.2 = **¥150.40**
- 成本变动日志自动记录，标记影响的项目

### 案例2：ODM项目整机报价管理

**背景**：某ODM显示器项目有4家工厂报价。

**操作步骤**：
1. 在项目管理中选择项目，切换到"整机供应商"标签页
2. 添加4家工厂报价：
   - 富士康：¥500.00，份额40%
   - 和硕：¥520.00，份额30%
   - 广达：¥510.00，份额20%
   - 纬创：¥515.00，份额10%
3. 系统自动计算加权报价：500×0.4 + 520×0.3 + 510×0.2 + 515×0.1 = **¥509.50**

**后续变动**：
- 富士康报价降至¥490.00
- 系统弹窗要求输入原因："量产规模扩大，成本下降"
- 加权报价更新为：490×0.4 + 520×0.3 + 510×0.2 + 515×0.1 = **¥505.50**
- 报价变动日志自动记录

### 案例3：成本变动追溯

**场景**：项目经理发现某项目成本突然上涨¥2.50，需要追溯原因。

**追溯步骤**：
1. 查询 `cost_change_log` 表，筛选该项目相关的记录
2. 发现 `change_type = 'project_bom_cost'` 的记录，显示器件"电源模块"成本上涨
3. 继续追溯该器件的 `cost_change_log` 记录
4. 发现 `change_type = 'part_supplier_price'`，供应商"台达电子"价格从¥25.00涨至¥28.00
5. 查看 `change_reason` 字段："铜价上涨导致原材料成本增加"
6. 确认影响范围：该电源模块用于3个项目，全部受到影响

---

## 六、常见问题

### Q1: 如何处理供应商份额总和不为100%的情况？
**A**: 系统自动归一化。例如份额为30%和20%，系统自动计算为60%和40%。

### Q2: 停用的供应商还会影响加权成本吗？
**A**: 不会。停用的供应商不参与加权成本计算，只有启用状态的供应商才参与。

### Q3: 修改供应商价格必须填写原因吗？
**A**: 是的。系统强制要求填写变动原因，不允许空白提交，以确保价格变动的可追溯性。

### Q4: 器件成本变化后，项目成本会自动更新吗？
**A**: 会。器件加权成本变化后，所有使用该器件的项目BOM成本会自动重新计算，并记录成本变动日志。

### Q5: 如何查看某个器件的成本变动历史？
**A**: 方法1：点击供应商卡片的"时钟"图标查看该供应商的价格历史；方法2：查询 `cost_change_log` 表，筛选该器件的成本变动记录。

### Q6: 整机供应商和器件供应商有什么区别？
**A**: 
- **器件供应商**：针对单个器件（如LCD面板、电源模块），管理器件级别的供应商报价。
- **整机供应商**：针对整个项目（如ODM工厂），管理项目级别的整机报价。

---

## 七、版本记录

### v2.3.2 (2026-07-23)
- 简化供应商字段（移除代码、交期、MOQ）
- 价格变动强制记录原因
- 新增整机供应商管理
- 新增成本变动追溯系统

### v2.3.1 (2026-07-23)
- 初版供应商管理系统
- 支持多供应商报价和份额分配
- 加权成本自动计算
- 供应商价格历史追踪

---

**文档版本**: v2.3.2  
**最后更新**: 2026-07-23  
**作者**: CostHub 开发团队
