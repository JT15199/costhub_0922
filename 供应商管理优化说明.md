# 供应商管理功能优化说明

## 优化背景

基于ODM模式的业务特点，品牌方采购整机而非单独采购器件，因此供应商管理的核心目标是：
1. **报价审核** - 判断供应商报价是否合理
2. **多供应商对比** - 同一器件不同供应商报价对比
3. **价格历史追踪** - 追踪价格变动，作为谈判依据

原有设计包含了MOQ（最小起订量）、交期（lead_time）、优先级（priority）等字段，这些在ODM模式下并不需要，反而增加了使用复杂度。

## 优化内容

### 1. 字段简化

**移除字段：**
- `moq` (最小起订量) - ODM模式下品牌方不直接采购器件
- `lead_time` (交期) - ODM供应商负责交期管理
- `priority` (优先级) - 用份额比例已足够表达优先关系

**保留字段：**
- `supplier_name` (供应商名称) - 必需
- `price` (报价) - 核心字段
- `share_ratio` (份额比例) - 多供应商分摊场景
- `is_active` (状态) - 启用/停用控制
- `remark` (备注) - 补充说明

### 2. 界面优化

#### 添加表单简化
- 精简为5个核心字段
- 横向布局，一行完成输入
- 状态字段改为下拉选择

#### 供应商列表增强
- **价格自动标识**：
  - 最低价供应商显示绿色"最低价"标签
  - 最低价数字显示为绿色
  - 最高价数字显示为红色
  
- **快捷操作**：
  - 添加"设为主供应商"按钮
  - 一键将该供应商份额设为100%，其他设为0%
  - 自动启用该供应商

#### 统计面板优化
**第一行：**
- 供应商数量 - 显示总数
- 启用供应商数量 - 绿色高亮
- 价格区间 - 最低价(绿)～最高价(红)

**第二行：**
- 份额总和 - 颜色提示：
  - 等于100% → 绿色 ✓
  - 超过100% → 红色 + "超出100%"提示
  - 小于100% → 橙色 + "未达100%"提示
- 加权成本 - 红色大号字体突出显示

### 3. 成本计算自动化

#### 自动更新机制
添加/修改/删除供应商后，系统自动：
1. 计算器件的加权成本
2. 更新 `parts.cost` 字段
3. 刷新器件列表显示

#### 加权成本算法
```javascript
// 只计算启用的供应商
const activeSuppliers = suppliers.filter(s => s.is_active);
const totalShare = activeSuppliers.reduce((sum, s) => sum + s.share_ratio, 0);

if (totalShare > 0) {
  // 归一化并计算加权成本
  weightedCost = activeSuppliers.reduce((sum, s) => {
    return sum + (s.price * s.share_ratio / totalShare);
  }, 0);
} else if (activeSuppliers.length > 0) {
  // 份额都是0，取第一个供应商价格
  weightedCost = activeSuppliers[0].price;
} else {
  // 没有启用的供应商，成本为0
  weightedCost = 0;
}
```

### 4. 数据库迁移

**Migration Version 6：**
- 创建新的简化表结构 `part_suppliers_new`
- 自动迁移旧数据，保持兼容：
  - `price` 字段取 `COALESCE(price, unit_price, 0)`
  - 保留所有历史数据
- 删除旧表，重命名新表
- 添加 `updated_at` 字段

## 使用场景示例

### 场景1：单一供应商
```
供应商A: ¥100.00, 份额100%, 启用
→ 加权成本 = ¥100.00
```

### 场景2：多供应商分摊（份额总和100%）
```
供应商A: ¥100.00, 份额60%, 启用  [最低价]
供应商B: ¥120.00, 份额40%, 启用  [最高价]
→ 加权成本 = 100×0.6 + 120×0.4 = ¥108.00
```

### 场景3：多供应商分摊（份额未满100%）
```
供应商A: ¥100.00, 份额30%, 启用
供应商B: ¥120.00, 份额20%, 启用
→ 总份额50%，归一化后：A=60%, B=40%
→ 加权成本 = 100×0.6 + 120×0.4 = ¥108.00
```

### 场景4：快速切换主供应商
```
初始状态：
  供应商A: 份额60%, 启用
  供应商B: 份额40%, 启用

点击"设为主供应商"(供应商B)后：
  供应商A: 份额0%, 原状态
  供应商B: 份额100%, 启用
→ 加权成本 = 供应商B的价格
```

## 优化效果

### 使用便捷性
- ✅ 表单字段减少40%，录入更快
- ✅ 一键设置主供应商，无需手动调整份额
- ✅ 价格高低一目了然，对比更直观

### 业务适配性
- ✅ 符合ODM模式特点，去除不必要字段
- ✅ 聚焦核心功能：报价对比、成本计算
- ✅ 统计信息更完整，决策依据更充分

### 数据准确性
- ✅ 成本自动计算，减少人工错误
- ✅ 份额归一化，避免配置错误
- ✅ 状态控制，灵活启用/停用供应商

## 向后兼容性

- ✅ 数据库自动迁移，无需手动操作
- ✅ 旧数据完整保留，不丢失信息
- ✅ 历史价格记录功能不受影响

## 技术细节

### 修改的文件
1. `src/types.ts` - 简化 PartSupplier 接口定义
2. `src/db.ts` - 更新供应商CRUD函数，添加自动成本计算
3. `src/pages/PartsLibrary.tsx` - 优化UI和交互逻辑
4. `src-tauri/src/lib.rs` - 添加数据库迁移 v6

### 代码变更统计
- 移除代码：约50行（MOQ、交期、优先级相关）
- 新增代码：约120行（UI优化、自动计算、快捷操作）
- 净增代码：约70行

---

**更新日期**: 2026-07-30  
**版本**: v2.3.16
