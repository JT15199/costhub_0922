# 安全注意事项

## 已知安全漏洞

### ⚠️ xlsx 库（版本 0.18.5）

**漏洞详情**：
- 原型污染漏洞 (CVE-2024-XXXX)
- 正则表达式拒绝服务 (ReDoS)

**当前状态**：
- xlsx 最新版本仍为 0.18.5
- 漏洞修复版本尚未发布
- npm audit 显示高危漏洞

**缓解措施**：

1. **限制 Excel 导入来源**
   - 仅导入可信来源的 Excel 文件
   - 避免导入来自网络的未知文件

2. **文件大小限制**
   - 限制导入文件大小（建议 < 10MB）
   - 避免导入包含大量数据的文件

3. **数据验证**
   - 导入前验证文件格式
   - 检查文件结构是否符合预期

4. **替代方案**
   - 考虑迁移到更安全的库：
     - **exceljs** - 功能完整，安全性好
     - **sheetjs-cjs** - SheetJS 社区版本

## 数据安全

### 数据库安全

1. **数据备份**
   ```bash
   # 手动备份
   copy monitor_cost.db monitor_cost_backup.db
   
   # 定期备份建议（可添加脚本）
   # 每日备份、每周备份
   ```

2. **数据库位置**
   - 数据库文件位于 exe 同目录
   - 避免数据库文件被误删或覆盖

3. **数据导出**
   - 定期导出关键数据到 CSV/JSON
   - 保留历史版本数据

### 输入验证

**前端验证**：
- 所有用户输入必须验证
- 限制输入长度和格式
- 防止注入攻击

**后端验证**：
- Rust 后端应进行二次验证
- 数据库查询使用参数化语句（已实现）

## 应用安全

### Tauri 安全配置

**当前配置**（src-tauri/tauri.conf.json）：
```json
{
  "app": {
    "security": {
      "csp": null  // ⚠️ 开放 CSP，仅适用于开发环境
    }
  }
}
```

**建议改进**：
```json
{
  "app": {
    "security": {
      "csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'"
    }
  }
}
```

### 权限控制

**capabilities/default.json**：
- 定义应用可访问的资源
- 限制危险的 API 调用

**建议**：
- 审查权限配置
- 仅保留必要的权限
- 禁用不需要的功能

## 网络安全

### 本地应用

此应用为**纯本地应用**：
- ✅ 无网络通信
- ✅ 数据不上传云端
- ✅ 无第三方 API 调用

### 未来扩展

如果添加网络功能：
- 使用 HTTPS 通信
- 添加身份认证
- 数据加密传输
- API 密钥保护

## 安全最佳实践

### 开发阶段

1. **代码审查**
   - 定期审查关键代码
   - 检查数据库操作
   - 验证输入处理

2. **依赖更新**
   - 定期检查依赖更新
   - 关注安全公告
   - 及时更新有漏洞的包

3. **测试覆盖**
   - 添加安全测试
   - 测试边界情况
   - 验证异常处理

### 运行阶段

1. **用户教育**
   - 提示用户备份数据
   - 说明文件导入风险
   - 提供安全使用指南

2. **错误处理**
   - 不暴露敏感信息
   - 优雅处理错误
   - 记录异常日志

3. **版本管理**
   - 维护版本历史
   - 提供回滚机制
   - 记录变更日志

## 安全检查清单

### 发布前检查

- [ ] npm audit 检查漏洞
- [ ] 审查数据库操作
- [ ] 验证输入处理
- [ ] 检查 CSP 配置
- [ ] 测试异常情况
- [ ] 备份测试数据

### 运行时监控

- [ ] 监控数据库大小
- [ ] 检查异常日志
- [ ] 用户反馈收集
- [ ] 性能指标监控

## 安全更新历史

### 2026-06-21
- ✅ 添加安全文档
- ⚠️ 发现 xlsx 0.18.5 漏洞（待修复）
- ✅ 数据库操作使用参数化查询
- ⚠️ CSP 配置开放（生产环境需收紧）

---

**维护者**：定期更新此文档，记录安全问题和修复措施。

**最后更新**：2026-06-21