# 安全注意事项

## 已知安全漏洞

### ✅ xlsx 库（版本 0.20.3）

**已修复漏洞**：
- 原型污染漏洞 (CVE-2023-30533)
- 正则表达式拒绝服务 (CVE-2024-22363)

**当前状态（2026-08-30）**：
- 已从停止更新的 npm registry 版本 0.18.5 切换到 SheetJS 官方分发 0.20.3
- 已修复原型污染与 ReDoS 已知漏洞
- npm audit 剩余告警来自 `pptxgenjs` 的 Node-only `image-size` 传递依赖；CostHub 使用浏览器构建且 PptxGenJS 将该依赖标记为 browser=false，当前导出路径不解析外部图片

**持续措施**：

1. **限制 Excel 导入来源**
   - 仅导入可信来源的 Excel 文件
   - 避免导入来自网络的未知文件

2. **文件大小限制**
   - 限制导入文件大小（建议 < 10MB）
   - 避免导入包含大量数据的文件

3. **数据验证**
   - 导入前验证文件格式
   - 检查文件结构是否符合预期

4. **依赖来源**
   - SheetJS 后续升级继续使用官方分发并核对发布说明
   - 不使用 `npm audit fix --force` 自动降级 PptxGenJS

## 数据安全

### 数据库安全

1. **数据备份**
   - 通过设置页的数据库备份执行 SQLite `VACUUM INTO`，生成一致性单文件快照。
   - 禁止在应用运行中直接分别复制 `.db`、`.db-wal`；旧版备份仅在恢复流程中兼容。

2. **数据库位置**
   - 数据库文件位于 exe 同目录
   - 避免数据库文件被误删或覆盖

3. **数据导出**
   - 定期导出关键数据到 CSV/JSON
   - 保留历史版本数据

### 输入验证

**前端验证**：
- 所有用户输入必须验证
- 限制输入长度和格式
- 防止注入攻击

**后端验证**：
- Rust 后端应进行二次验证
- 数据库查询使用参数化语句（已实现）

## 应用安全

### Tauri 安全配置

**当前配置**（src-tauri/tauri.conf.json）：
```json
{
  "app": {
    "security": {
      "csp": { /* 已限制为本地资源、Tauri IPC、data/blob 图片和 worker */ }
    }
  }
}
```

**当前原则**：
```json
{
  "app": {
    "security": {
      "csp": "以 src-tauri/tauri.conf.json 的最小权限配置为准"
    }
  }
}
```

### 权限控制

**capabilities/default.json**：
- 定义应用可访问的资源
- 限制危险的 API 调用

**建议**：
- 审查权限配置
- 仅保留必要的权限
- 禁用不需要的功能

## 网络安全

### 本地应用

此应用采用**本地数据 + 受控云端洞察**：
- ✅ 本地数据库没有提供给外部服务的读取通道
- ✅ 本地模型可读库；外部 LLM/搜索仅经 Rust HTTP 转发
- ✅ 云端外发只允许物料名/品类/问题三字段，发送前严格脱敏并留审计记录
- ✅ 招标报价原文、供应商报价批次、规格基线和比价矩阵只写入本机 SQLite；Excel 解析器不包含网络调用
- ✅ `query_tender_analysis` 是只读白名单工具，模型只能读取聚合报价，不能覆盖 BOM 或直接生成采购篮子
- ✅ 招标报价导入采用“预览→确认”，规格冻结、批次导入和匹配确认均写入本地过程事件

### 招标数据外发禁止项

- 原始报价 Excel、供应商名称、型号、金额、BOM 行和项目代号不得进入云端请求、外链 URL、遥测或错误日志。
- 若本地模型申请云端补充公开知识，必须继续使用 Rust `cloud_http_request` 的 HTTPS 白名单、敏感字段拦截和条件审批；招标数据默认不满足外发条件。

### 未来扩展

如果添加网络功能：
- 使用 HTTPS 通信
- 添加身份认证
- 数据加密传输
- API 密钥保护

## 安全最佳实践

### 开发阶段

1. **代码审查**
   - 定期审查关键代码
   - 检查数据库操作
   - 验证输入处理

2. **依赖更新**
   - 定期检查依赖更新
   - 关注安全公告
   - 及时更新有漏洞的包

3. **测试覆盖**
   - 添加安全测试
   - 测试边界情况
   - 验证异常处理

### 运行阶段

1. **用户教育**
   - 提示用户备份数据
   - 说明文件导入风险
   - 提供安全使用指南

2. **错误处理**
   - 不暴露敏感信息
   - 优雅处理错误
   - 记录异常日志

3. **版本管理**
   - 维护版本历史
   - 提供回滚机制
   - 记录变更日志

## 安全检查清单

### 发布前检查

- [ ] npm audit 检查漏洞
- [ ] 审查数据库操作
- [ ] 验证输入处理
- [ ] 检查 CSP 配置
- [ ] 测试异常情况
- [ ] 备份测试数据

### 运行时监控

- [ ] 监控数据库大小
- [ ] 检查异常日志
- [ ] 用户反馈收集
- [ ] 性能指标监控

## 安全更新历史

### 2026-08-30
- ✅ SheetJS 升级至官方 0.20.3，移除未使用的 react-router-dom
- ✅ Tauri CSP 从 null 收紧
- ℹ️ 按用户明确要求保留单机密码防遗忘机制：SHA-256 校验值 + 本机明文副本，双击登录页 Logo 可查看
- ✅ 数据库备份改为 SQLite 一致性快照
- ✅ 普通 Rust HTTP 出口只允许 127.0.0.1 / ::1；Ollama 读取成本数据前必须验证 Windows 出站防火墙隔离
- ✅ 云端恢复为专用受控网关：敏感审查 + 条件审批 + HTTPS 域名白名单 + 请求体字段/大小校验 + 禁止重定向
- ✅ 移除 shell 外链权限；来源地址只能复制，不在应用内打开
- ✅ 本地 Skill 图表只接收查询条件，图表数据由代码从数据库重新聚合，不信任模型生成数字
- ✅ Ollama 出站防火墙状态查询与“一键锁定”脚本均以无窗口 PowerShell 后台执行；仅在用户主动加固时保留系统 UAC 授权提示，不弹出控制台

### 2026-06-21
- ✅ 添加安全文档
- ⚠️ 发现 xlsx 0.18.5 漏洞（待修复）
- ✅ 数据库操作使用参数化查询
- ⚠️ CSP 配置开放（生产环境需收紧）

---

**维护者**：定期更新此文档，记录安全问题和修复措施。

**最后更新**：2026-08-31
