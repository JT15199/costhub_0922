# CostHub 物料成本洞察模块 · 分析框架扩充与结果展示修复 v6

> 本文档包含：① 新增"五看分析法"预置Skill；② 一个新发现的Markdown渲染bug；③ 对v5文档第二节"洞察结果结构化重排"、v3文档第9节"来源链接可点击"两项需求**仍未落地**的重申；④ 结果展示区域的排版层级细化要求。
> 请与 v3、v5 文档配合阅读。

---

## 一、新增预置Skill：五看分析法

### 1.1 框架来源

"五看三定"是华为战略管理框架中的战略洞察部分，包含"看行业/趋势、看市场/客户、看竞争、看自己、看机会"五个分析角度，是公开、成熟的商业分析方法论。本节将其改造为适用于物料成本洞察场景的版本。

### 1.2 改造后的分析维度

| 原"五看" | 改造后对应的分析角度 |
|---|---|
| 看行业/趋势 | 宏观技术趋势、行业整体供需大势 |
| 看市场/客户 | 下游需求端（哪些产品/应用场景在拉动该物料的需求） |
| 看竞争 | 供给端格局（供应商数量、产能集中度、议价空间） |
| 看自己 | 转化为"这对我方采购意味着什么"——议价地位、替代方案可行性、是否需要囤货 |
| 看机会 | 关键时机窗口——出现哪些信号意味着当前适合锁价或适合观望 |

### 1.3 与既有Skill的关系

- "看竞争"这一维度与此前设计的"竞争格局分析"（基于波特五力框架）内容有重叠，建议在实现时评估是否合并为一个更完整的Skill，避免出现两个高度相似的预置选项让用户难以选择
- "看自己""看机会"是波特五力等纯外部分析框架所缺少的、"分析后如何决策"这一层，建议作为"综合深度研究"这个预置Skill的核心框架基础，替代当前含糊的"多维度全面分析"描述

### 1.4 落地要求（呼应此前"Skill显式化"设计原则）

该Skill需按 v3 文档已确立的原则，实现为一份独立、可读、可编辑的 `.md` 文件（如 `skills/five-views-analysis.md`），而非硬编码在代码中的字符串，并可通过"查看Skill内容"入口在界面上完整展示。

---

## 二、新发现Bug：Markdown 未被正确渲染

**现象**：洞察结果文本中，AI返回内容里的加粗标记（如 `**产业链涨价潮密集**`）被原样显示为带星号的纯文本，未被解析成实际的加粗效果。

**原因**：结果展示组件直接将AI返回的原始文本输出到界面，没有经过Markdown渲染处理。

**修复要求**：结果展示区域需引入Markdown渲染组件（如 `react-markdown` 或同类库），将AI返回的Markdown格式文本正确渲染为对应的富文本样式（加粗、列表等），不得以纯文本形式直接展示原始Markdown语法字符。

---

## 三、重申：洞察结果结构化重排尚未实现

**背景**：v5 文档第二节已明确要求，洞察结果应按"供需格局/价格驱动/卡点识别/政策地缘/展望"五个维度分类展示，每条信息附证据强度标签（强/中/弱/未验证）。

**现状确认**：最新截图显示，洞察结果仍是扁平的编号列表（1. 2. 3. 4. 5.），未按维度分类，也没有证据强度标签。**该需求尚未落地，需要重新排期实现**，请勿在后续开发中遗漏或视为已完成。

---

## 四、重申：来源链接缺失，未实现可点击打开

**背景**：v3 文档第9节已明确要求，洞察结果中的来源链接需支持点击后通过系统默认浏览器打开（使用 Tauri 的 `opener` 插件或对应 `shell.open()` API）。

**现状确认**：最新截图显示，来源信息目前仅以"（来源：XX，2026年X月）"这样的纯文字形式嵌在正文里，**既不是独立展示的链接，也无法点击打开**。这个需求同样尚未落地。

**修复要求**：
1. 每条来源信息需要展示为独立的、可视觉识别的链接元素（而非嵌在句子中间的纯文字），建议采用第五节排版方案中的角标或小图标形式
2. 点击后通过系统默认浏览器打开对应URL
3. 这一要求同时适用于洞察结果各维度内容、关键事件列表（`trend_key_events`）等所有涉及信息来源的展示位置，需要统一实现，不要出现部分位置支持点击、部分位置仍是纯文本的不一致情况

---

## 五、结果展示区域排版层级要求

在完成第三节的维度分类和第四节的来源链接修复基础上，进一步明确视觉层级设计，解决当前"所有文字字号字重一致、难以快速抓取重点"的问题：

| 层级 | 内容 | 样式要求 |
|---|---|---|
| 一级 | 维度标题（如"供需格局"） | 字号最大、加粗，可搭配小图标 |
| 二级 | 正文描述 | 正常字重；文中出现的关键数字（如涨跌百分比）需用醒目颜色或加粗突出，与普通描述文字形成区分 |
| 三级 | 来源标注 | 字号最小、使用弱化的灰色调，与正文明显拉开视觉重量，避免与结论内容互相争夺注意力 |
| 徽章 | 证据强度标签（强/中/弱/未验证） | 做成小色块/小徽章形式，不use纯文字堆砌，参考现有"上涨""高置信"标签的呈现方式 |

---

## 六、验收标准

- [ ] 新增"五看分析法"作为预置Skill选项，内容按1.2节五个维度组织，以独立`.md`文件形式存在且可在界面查看
- [ ] "看竞争"维度与既有"竞争格局分析"Skill的重复内容已评估处理（合并或差异化说明）
- [ ] 洞察结果文本中的Markdown语法被正确渲染，不再出现原始星号等语法字符
- [ ] 洞察结果按五个维度分类展示，每条信息附带证据强度标签（此为v5已有要求，本次作为未完成项重新确认）
- [ ] 所有来源信息展示为独立可点击元素，点击后通过系统默认浏览器打开，覆盖洞察结果、关键事件等所有涉及来源的位置
- [ ] 结果展示区域的字号、字重、颜色按五节表格中的层级要求实现，关键数字有视觉突出
