# 免安装首次启动与 Agent 固定计算返修（2026-09-17）

## 首次启动
根因是 Rust SQLite 连接未开启 create_if_missing，旧机器已有库时不暴露问题。此前前端内存 SQLite 测试覆盖了初始化竞态，却没有覆盖后端在磁盘上首次创建文件，这是上轮验证的遗漏。

已修复建库选项，并按真实文件路径打开数据库，避免 # 等字符被作为 URL 片段解析。登录页显示初始化状态；失败持久显示具体路径/原因及重试入口。不会删除数据库、覆盖已有密码或绕过登录。

真实 EXE 验证：`node docs/check-portable-first-run.cjs`，仅将 EXE 复制到新的临时目录“中文 # 空格”，使用真实 WebView2 和 Rust SQL，无模拟调用。检查 costhub.db 创建、默认账号/密码哈希、首次登录和重启登录均成功。结果及 EXE SHA-256 见 portable-first-run-result.json；未接触正式业务数据库。

## Agent
复用 query_project_bom，新增 view=rank/summary/group，覆盖最高最低、Top N、均值、总额和模块/类别统计。先全量计算再截取结果。单价与数量乘单价明确区分；缺项不当零、并列不称唯一。完整项目名称或规范化代号可直接查询。

Pi 工具提示改为固定计算交给工具；相同参数连续成功只读查询在同轮 30 秒内复用上次证据，第三次进入结论。不会缓存失败；业务写入、原生文件/命令调用清除复用记录。不擅自关闭用户选择的深度思考，不额外限制模型输出。

## 验证
- 前端：394 项通过，2 项跳过。
- Rust SQL 边界：3 项通过，包括磁盘首次创建/重开及中文特殊路径。
- 10,002 行 BOM 测试最大值在第 10001 行，验证全量而非单页排名；返回摘要少于 2500 字符。另覆盖小计排名、模块汇总、缺失数据、并列和空筛选。
- Pi 模拟传输验证原生排名 schema 预加载、参数键顺序无关、连续重复只执行一次并收尾。
- Tauri release EXE、MSI、NSIS 构建成功。

27B 实机生成速度与真实模型选工具质量未在本机测量；当前通过的是工具计算、调度防重复及真实桌面首次启动。收件人目录若不可写，会显示明确错误，应将 EXE 放在本机可写文件夹，不应关闭公司的安全保护。
