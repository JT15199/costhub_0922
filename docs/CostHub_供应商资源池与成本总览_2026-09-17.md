# 本轮交付记录（2026-09-17）

- 资源池：供应商管理 → 供应商资源池。可维护名称、供应范围、联系人、电话和备注；项目整机供应商、器件供应商及报价导入共用候选名称，仍允许直接输入。
- 整机设置：项目管理 → 选中项目 → 报价与定点 → 供应商定点。
- 地图：供应商管理 → 整机供应商（ODM）→ 供应商地图。复用多厂区档案，缺少定位时显示维护地址入口，不发送业务数据查询外部地图。
- 总览：今日工作台 → 成本总览。所有未删除项目可搜索、排序、按状态筛选、分页查看，并点击项目进入详情。单台 BOM 与带平台费率成本并列，沿用项目标准成本口径；利润不计入，缺价格或数量保留未知。
- 登录：首次建表使用原始连接，初始化完成后才提供连接；并发入口复用初始化任务，密码校验等待默认密码写入。异常直接报错，不再混同密码不匹配。已有账号密码不重置。

## 验证

- `npm test`：71 文件通过，392 项通过、2 项跳过。
- `src/__tests__/authStartup.test.ts`：真实内存 SQLite 执行完整 schema，验证并发业务访问/初始化/默认密码登录、错误密码、改密后重新连接、数据库错误及重试。Tauri IPC 用内存 SQLite 替代，不读写用户数据库。
- `node docs/check-supplier-resources.cjs`：真实组件与持久化入口、模拟 SQL 数据，验证资源新建/编辑/重载、两个选择入口及直接输入整机名称后在器件入口复用。
- `node docs/check-supplier-map.cjs`：真实供应商页面、模拟数据，验证 ODM 多厂区落点、项目数量、编辑指定厂区后刷新、待定位入口。
- `node docs/check-dashboard-layout.cjs`：真实工作台组件、模拟数据，验证六档布局、成本总览/搜索/费率/跳转，以及原有柱图分页与文字选择回归。
- UI 截图：cost-overview-acceptance.png、supplier-map-odm-acceptance.png、supplier-resource-acceptance.png。

收件人电脑上的实际运行尚未实测；已有 monitor_cost.db 的目录应使用该数据库原来的密码，不会因替换 EXE 恢复为默认密码。

发布构建：npm run tauri build 退出 0，已更新 src-tauri/target/release/costhub.exe 和 MSI/NSIS；EXE 最后写入时间 2026-09-17 15:40:18。
