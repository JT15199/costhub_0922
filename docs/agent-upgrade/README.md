# CostHub 通用 Agent 升级：交给 Luna 的入口

方案日期：2026-09-10。工作目录：`D:\AI coding folder\costhub`。

这是实施任务，不是再次写方案。用户要：保留免安装桌面应用，做出能查资料、调用工具、写脚本、自行纠错、自动压缩上下文、恢复任务、按需脱敏咨询云端的通用 Agent；成本是专业方向，不能把行为写成一组固定业务流程。界面保持简单。主要本地环境为 Ollama、Qwen3.8 27B、24GB 显存；实际模型标签、量化和可运行上下文必须检测。

## 给新对话的启动指令

复制下面这段交给 Luna，并让该对话使用本项目目录：

> 请实施 `docs/agent-upgrade/README.md` 指向的完整升级。先读项目 AGENTS.md、本入口和 PROGRESS.md，再按 IMPLEMENTATION.md 的阶段依次完成。你要修改代码、自己运行测试、做真实模型和桌面验收、交付便携包，不要只交计划或演示。
>
> 先盘点已有工作，保留所有用户数据和非本任务改动。优先复用已安装 Pi 0.84.4 的 AgentHarness、会话和压缩能力；实际 API 以本地依赖为准。不能用静态关键词工具路由、丢弃历史、模拟工具结果或只通过 mock 测试冒充完整 Agent。
>
> 所有验收项以 ACCEPTANCE.md 为准。每完成一小步先更新 PROGRESS.md 的状态、证据和唯一下一步；上下文压缩后先重新读取入口与进度，核对文件再继续，不能重做已完成内容或漏掉前面的约束。阶段完成后继续下一阶段，不必逐阶段等待我批准。
>
> 日常界面只保留对话、附件、发送/停止、任务/文件和必要确认；自动压缩、工具发现、模型调度在后台完成。正式数据库写入、出域、非隔离本机命令仍按本任务方案的明确权限执行。真实云端验收使用合成数据和已有授权/预算；没有授权或凭据就标为未验收，不伪造通过。
>
> 最终必须交付真实验收记录、可打开的产物、带运行环境的便携包和已知限制。任何硬性验收没有通过，都不能说“完整通用 Agent 已完成”。不要因工作量大缩成按钮加提示词的版本。

## 只维护四份交付文档

| 文件 | 用途 | 阅读方式 |
|---|---|---|
| `README.md` | 用户意图、执行纪律和入口 | 每次恢复必读 |
| `PROGRESS.md` | 当前状态、决策、证据、唯一下一步 | 每次恢复必读；事实优先于记忆 |
| `IMPLEMENTATION.md` | 产品行为、架构、上下文算法、P00—P09 实施步骤 | 第一次读第 0—3 节，随后只读当前阶段与引用章节 |
| `ACCEPTANCE.md` | 可重复测试、真实模型任务、交付硬门槛 | 先读总规则；每阶段读对应测试 |

不要每次把整个代码库和整份方案塞进上下文。用 `rg -n '^## |^### ' docs/agent-upgrade/IMPLEMENTATION.md` 找章节，再用 PowerShell `Get-Content ... | Select-Object -Skip <起始行减1> -First <行数>` 精确读取。来源行号会变化，按函数名和章节定位。

## 执行纪律

1. 所有条目最初都是“未开始”，方案中的“要求/建议”不是已经实现。
2. P00—P09 是完整交付的连续阶段，不是选做列表。低成本 mock 测试、真实工具测试、真实模型测试分开记录。
3. 不新开分支覆盖当前未提交工作，不 `reset --hard`、不批量恢复文件。先读取，再做最小范围修改；新建文件前查同名和导出引用，尤其不要覆盖已有 `src/quoteReview.ts`。
4. 不把失败记录清零后重跑冒充首次成功；记录首次结果、修复与最终结果。
5. 技能/依赖里已有能力先复用。发现本方案指定 API 与本地版本不符，记录证据和替代决定，再继续；不猜 API、不无故升级全套依赖。
6. 能独立推进的代码/合成数据验证先做完。只有凭据、外发授权、不可用硬件等真实阻碍才向用户询问；不把普通实施选择变成审批。
7. 前面的书架、手账追加/编辑、锁修复、报价审核简化是既有成果，必须回归，不能被本次 AI 重构破坏。
8. 本任务没有授权自动联系供应商、发送邮件、上传原始业务资料或开启无限后台云端消费。

## 最终体验的验收句

用户放入两份报价，提出任务；Agent 自己读文件、查本地项目、写并修正脚本、解释可比性、生成可打开的结果。用户中途改要求，它继续执行；上下文快满时自动整理并续做；退出后恢复时保留任务事实与未完成项，未知写入不会重复。需要云端时只发送经过审查的最小信息，获得新证据才继续下一轮。用户无需记工具名或切换一排业务模式。

