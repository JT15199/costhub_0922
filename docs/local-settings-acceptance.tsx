import React from 'react';
import { createRoot } from 'react-dom/client';
import LocalBackendSettings from '../src/components/LocalBackendSettings';
createRoot(document.getElementById('root')!).render(<LocalBackendSettings />);
