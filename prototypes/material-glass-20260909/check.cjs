const { chromium } = require('C:/Users/96529/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert = require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,args:["--disable-gpu"]});const page=await browser.newPage({viewport:{width:1600,height:1000},deviceScaleFactor:1});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:5173/prototypes/material-glass-20260909/');await page.locator('#inspector h2').waitFor();assert.equal(await page.locator('#inspector h2').textContent(),'驱动 IC');await page.waitForTimeout(400);await page.screenshot({path:__dirname+'/overview.png'});await page.locator('.node[data-node="tft"]').click();assert.equal(await page.locator('#inspector h2').textContent(),'TFT-LCD 面板');await page.locator('[data-tab="skills"]').click();assert.equal(await page.locator('#skills-content').isVisible(),true);await page.locator('#minus').click();assert.equal(await page.locator('#zoom').textContent(),'90%');await page.locator('#focus').click();assert.equal(await page.locator('.upstream').first().isVisible(),false);assert.deepEqual(errors,[]);console.log('PASS: node details, dimensions, zoom, collapse; no page errors');await browser.close()})();


