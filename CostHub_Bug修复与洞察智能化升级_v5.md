# CostHub 物料成本洞察模块 · Bug修复与洞察智能化升级 v5

> 本文档基于实际使用中发现的问题整理，包含三部分：① 三个具体bug的修复要求；② 洞察结果的结构化重排（借鉴Serenity方法论）；③ 分析清单机制从"用户确认制"调整为"自主记忆制"，**替代 v3 文档第7节的原有设计**。
> 请与 v3、v4 文档配合阅读，本文档内容与前序文档冲突处，以本文档为准。

---

## 一、Bug 修复清单

### Bug 1：批量AI拆解子件绕过草稿确认流程

**现象**：单个节点执行"AI拆解子件"时，结果会先生成草稿等待用户点击"确认草稿"；但执行**批量**AI拆解时，生成的子节点被直接当成已确认状态写入树，跳过了草稿确认这一步。

**修复要求**：批量拆解必须复用与单个拆解完全相同的"先生成草稿、等待确认"逻辑。生成的所有草稿应统一进入待确认队列，通过已有的"批量确认"按钮完成确认，不允许任何路径下的AI生成内容未经确认就直接写入正式数据。

### Bug 2：子节点成本占比（cost_ratio_estimate）语义不明确，数据不自洽

**现象**：同一父节点下的多个直接子节点，其成本占比字段之和既不等于100%，也不等于父节点自身的占比，数值明显是AI随意给出、未经校验的。

**修复要求**：
- 明确定义 `cost_ratio_estimate` 的语义为**"相对于直接父节点的占比"**
- 强制校验规则：同一父节点下所有直接子节点的该字段之和必须等于100%（允许合理误差，如99%-101%）
- AI生成拆解草稿后、用户确认前，需执行该校验：若总和明显偏离100%，界面提示"子节点成本占比总和为XX%，与100%不符，请检查或调整"
- 提供"自动按比例归一化"快捷操作（等比例缩放使总和精确等于100%），但不允许在未通过校验的情况下直接确认
- 相对于顶层根节点的绝对占比，通过逐级相乘计算得出，不单独存储，避免多处数据源不一致

### Bug 3：node_type 修改不生效

**现象**：① 批量拆解出的终端物料节点在图中显示为"结构"而非"终端"（大概率是 Bug 1 的连锁后果，`node_type` 停留在数据库默认值 `structural`，因为未经过确认环节被正确赋值）；② 用户手动将某节点编辑为"终端"并保存后，界面刷新仍显示"结构"（独立问题，需单独排查）。

**排查顺序**：
1. 确认编辑表单提交的数据是否包含 `node_type` 字段，是否被遗漏未提交
2. 确认保存/确认操作的后端逻辑，是使用用户当前编辑值写库，还是错误地使用了 `ai_suggested_node_type`（AI原始建议值）覆盖了用户的修改
3. 确认保存成功后，前端是重新从数据库拉取最新数据渲染，还是仍在使用保存前的本地缓存/状态

**建议**：优先修复 Bug 1，验证现象①是否随之消失；现象②需独立排查，两者原因不完全相同，不能假设修复其中一个另一个会自动解决。

---

## 二、洞察结果结构化重排

### 2.1 问题

当前洞察结果以扁平编号列表呈现（1. 2. 3. 4. 5.），信息混杂、字数偏多，难以快速抓住重点，也无法区分不同信息的可信程度。

### 2.2 方法论参考

参考公开的"供应链卡点"投资研究方法论（Serenity / @aleabitoreddit 公开内容中可观察到的研究路径，已被社区操作化为多个开源 Agent Skill，如 `serenity-skill` 等）中的两个核心设计，将其适配到物料成本洞察场景，**不直接安装该第三方skill**（其定位是股票/基金研究，与本工具的物料成本分析目标不同），仅借鉴方法论结构：

- **价值链分层拆解**思路——与本工具已有的"物料分解树"天然契合，不需要额外设计
- **卡点/瓶颈识别标准**——低供应商数量、长验证周期、扩产困难、客户认证严格、材料纯度要求高等，作为判断价格驱动因素的具体维度参考
- **证据分级系统**——将信息来源按可信程度分级，而非无差别罗列

### 2.3 新的结果展示结构

洞察结果改为按以下维度分类展示，每条信息附带**证据强度标签**：

| 维度 | 内容 |
|---|---|
| **供需格局** | 当前供给紧张/宽松程度、扩产难度 |
| **价格驱动** | 原材料成本传导、技术或良率变化、产能利用率等具体驱动因素 |
| **卡点识别** | 是否存在低供应商数量、长验证周期、扩产困难、客户认证严格等"卡脖子"环节 |
| **政策/地缘** | 关税、产业政策、地缘风险等外部因素（无相关信息时该维度可留空，不强行填充） |
| **展望** | 短期（1-3月）/中期（半年）价格预期，及可能触发趋势反转的关键信号 |

**证据强度标签**：每条信息标注"强/中/弱/未验证"，判断标准参考：
- 强：多个独立信源交叉印证，且为近期一手信息（如企业公告、行业权威机构报告）
- 中：单一信源，但来源可信度较高（如主流财经媒体）
- 弱：来源模糊或信息较旧
- 未验证：搜索结果中提及但无法交叉核实

**来源链接**：每条信息后附来源链接，点击后通过系统默认浏览器打开（此要求已在 v3 文档第9节提出，本次重申并明确适用于洞察结果的每一条分维度信息，不仅是笼统的"来源列表"）。

### 2.4 数据结构调整

```sql
-- 洞察结果按维度拆分存储，替代原先单一的长文本摘要
CREATE TABLE trend_insight_dimensions (
    id INTEGER PRIMARY KEY,
    trend_snapshot_id INTEGER NOT NULL,
    dimension_type TEXT NOT NULL,       -- '供需格局'/'价格驱动'/'卡点识别'/'政策地缘'/'展望'
    content TEXT NOT NULL,
    evidence_strength TEXT NOT NULL,    -- '强'/'中'/'弱'/'未验证'
    source_title TEXT,
    source_url TEXT,
    FOREIGN KEY (trend_snapshot_id) REFERENCES trend_snapshots(id)
);
```

原 `trend_key_events`（关键事件表，见 v3 文档第5.2节）保留，作为跨维度的时间线事件记录，与本节的分维度结构并存，不互相替代——`trend_key_events` 回答"发生过什么、什么时候"，`trend_insight_dimensions` 回答"从哪些角度看、结论是什么"。

---

## 三、搜索轮次自适应机制

### 3.1 问题

当前无论信息是否充分，固定执行三轮搜索，造成不必要的浪费。

### 3.2 调整方案

- 默认执行**一轮**搜索
- 搜索完成后，AI基于以下因素自评信息充分度：信源数量、信源之间是否存在明显冲突、是否命中2.3节列出的关键维度（供需/价格驱动/卡点/展望等）
- 若自评认为信息不足，在结果展示区明确提示，例如："当前信息置信度中等，供需格局维度信息较少，建议补充搜索"，并提供"再搜索一轮"的按钮
- 是否补充搜索由用户决定，系统不自动追加轮次
- 每次补充搜索都基于同样的自评逻辑判断是否需要再次提示，不设固定轮次上限，但也不自动无限循环，始终需要用户主动触发下一轮

---

## 四、分析清单机制：由"用户确认制"调整为"自主记忆制"

**本节替代 v3 文档第7节的原有设计。**

### 4.1 调整说明

v3 文档中原设计要求"清单不自动添加，需用户手动确认"。经过重新讨论，**改为AI识别到重复追问模式后自动生效，不再要求逐条用户确认**，但需要配套设计保证透明度和可控性，避免变成不可追溯的黑箱。

### 4.2 核心原则

1. **自动生效，但可追溯**：每条自动生成的记忆，必须能查看到它是基于哪几次具体追问产生的，不允许出现"凭空"的规则
2. **强度分级，渐进生效**：不是一次追问就固化成规则，而是有一个从"观察中"到"稳定记忆"的自然升级过程
3. **记忆清单页面从"审批队列"变为"透明日志"**：用户不需要逐条批准，但可以随时查看当前有哪些记忆在生效、何时产生、可随时手动关闭/删除
4. **严格限定记忆的作用范围**：只影响"分析维度偏好"（用户关注哪些角度），不允许影响"具体结论判断"（不能让AI因为记住用户过去的判断倾向，而扭曲新一轮分析的客观性）

### 4.3 强度分级规则

| 触发次数 | 状态 | 说明 |
|---|---|---|
| 1-2次 | `observing`（观察中） | 记录但不影响实际分析提示，仅展示在日志中 |
| 3-4次 | `active`（已生效） | 自动纳入后续洞察查询的提示词，权重较低 |
| 5次以上 | `stable`（稳定记忆） | 正式固化，权重较高，升级过程本身留痕记录 |

### 4.4 数据结构

```sql
CREATE TABLE analysis_checklist (
    id INTEGER PRIMARY KEY,
    item_description TEXT NOT NULL,          -- 如"预计涨跌持续时间"
    strength_level TEXT NOT NULL DEFAULT 'observing',  -- observing / active / stable
    trigger_count INTEGER DEFAULT 1,
    is_active BOOLEAN DEFAULT 1,             -- 用户可随时手动关闭，关闭后立即停止影响分析提示
    source TEXT DEFAULT 'auto_extracted',
    first_triggered_at DATETIME,
    last_triggered_at DATETIME,
    updated_at DATETIME
);

-- 记忆与具体触发追问的关联，保证可追溯、可审查
CREATE TABLE checklist_trigger_log (
    id INTEGER PRIMARY KEY,
    checklist_item_id INTEGER NOT NULL,
    trend_item_id INTEGER,                   -- 触发该记忆的具体物料
    question_snippet TEXT,                   -- 追问原文片段
    created_at DATETIME,
    FOREIGN KEY (checklist_item_id) REFERENCES analysis_checklist(id)
);
```

### 4.5 明确不做的事（边界约束）

- 不进行真正的模型微调/训练，"记忆"始终是结构化的提示词补充清单，不改变底层模型
- 记忆内容不得影响客观事实类判断（如不能因为记住用户偏好乐观解读，而系统性弱化风险提示）
- 每条记忆必须支持彻底删除，删除后立即从后续分析提示中移除，不允许软删除后仍在后台生效
- 记忆清单页面需可随时完整查看，不做"隐藏在设置深处"式的处理，用户应能方便地了解系统当前"记住了什么"

---

## 五、验收标准

- [ ] 批量AI拆解子件的结果，与单个拆解一样先进入草稿状态，需经"批量确认"操作才写入正式数据
- [ ] 同一父节点下所有直接子节点的成本占比之和校验为100%±1%，不满足时阻止直接确认并提示，提供一键归一化选项
- [ ] node_type 字段的用户手动编辑能正确保存并在界面刷新后正确展示
- [ ] 洞察结果按"供需格局/价格驱动/卡点识别/政策地缘/展望"五个维度分类展示，每条附证据强度标签和可一键打开的来源链接
- [ ] 搜索默认执行一轮，AI自评信息不足时提示用户可选择再搜索一轮，不自动固定跑三轮
- [ ] 分析清单条目在达到3次以上重复追问后自动生效（无需用户逐条确认），但用户可随时查看每条记忆的触发依据并手动关闭
- [ ] 记忆内容仅影响分析维度覆盖范围，不影响具体涨跌方向等客观判断的独立性
