# 项目成本快照功能重新设计

## 用户需求

**核心场景**：
- 项目开发过程中BOM经常调整
- 需要记录不同版本的成本数据
- 对比不同版本之间的差异
- 查看历史配置回溯问题

## 功能设计

### 1. 快照列表管理

**项目详情页新增"快照历史"Tab**：
```
┌─────────────────────────────┐
│ 项目详情                     │
│ [基本信息] [BOM] [评审] [快照] │ ← 新增Tab
├─────────────────────────────┤
│ 快照历史                     │
│                              │
│ [保存当前快照] 按钮           │ ← 手动保存
│                              │
│ 快照列表：                   │
│ v1.0 | 初始版本 | ¥511 | 查看/对比 │
│ v1.1 | 优化后  | ¥498 | 查看/对比 │
│报价版|报价前   | ¥511 | 查看/对比 │
└─────────────────────────────┘
```

### 2. 快照详情查看

**点击"查看"显示历史版本BOM**：
```
快照：v1.0（初始版本）
保存时间：2026-06-20
总成本：¥511

BOM列表：
┌────────────────────────────┐
│ 器件名称 | 型号 | 成本 | 数量│
│ 面板IPS  | PN-238 | ¥280 | 1 │
│ Scaler   | TSUMC  | ¥15  | 1 │
│ ...                       │
└────────────────────────────┘
```

### 3. 快照对比功能

**选择两个快照对比差异**：
```
对比：v1.0 vs v1.1

成本变化：¥511 → ¥498 (-¥13)

器件变化：
┌───────────────────────────────┐
│ 器件     | v1.0成本 | v1.1成本 | 差异 │
│ Scaler   | ¥15     | ¥12      | -¥3 ↓ │
│ DDR      | ¥5      | ¥8       | +¥3 ↑ │
│ 电容100个| ¥5      | ¥3       | -¥2 ↓ │
│ 总差异   |         |          | -¥13 │
└───────────────────────────────┘

备注：优化Scaler方案，DDR容量升级
```

### 4. 快照数据结构

每个快照保存：
```json
{
  "snapshot_id": 1,
  "project_id": 123,
  "snapshot_name": "v1.0",
  "snapshot_desc": "初始版本",
  "total_cost": 511,
  "bom_items": [
    {
      "part_id": 1,
      "part_name": "面板IPS",
      "part_model": "PN-238",
      "cost": 280,
      "quantity": 1,
      "module_name": "核心部件"
    },
    ...
  ],
  "created_at": "2026-06-20 10:00:00"
}
```

## UI实现位置

### 项目详情页

**位置**：Projects.tsx 项目详情模态框

**Tabs结构**：
```tsx
<Tabs>
  <TabPane tab="基本信息" key="basic">
  <TabPane tab="BOM配置" key="bom">
  <TabPane tab="评审记录" key="review">
  <TabPane tab="降本措施" key="measures">
  <TabPane tab="目标成本" key="targets">
  <TabPane tab="快照历史" key="snapshots"> ← 新增
</Tabs>
```

### 快照历史Tab内容

**组件结构**：
- 顶部："保存当前快照"按钮 + 版本名称输入框
- 中部：快照列表表格
- 底部：对比按钮（选择两个快照对比）

### 快照对比模态框

**功能**：
- 左侧：旧版本快照BOM
- 右侧：新版本快照BOM
- 中间：差异标记（增删改）

## 实现步骤

### 步骤1：修改现有cost_snapshots表

**优化字段**：
- snapshot_name：版本名称（如v1.0、报价版）
- bom_data：完整BOM数据（JSON）
- 不再只保存总成本，保存完整BOM明细

### 步骤2：添加快照相关函数

**API函数**：
```typescript
// 保存项目快照（含完整BOM）
saveProjectSnapshot(projectId, name, desc)

// 获取项目快照列表
getProjectSnapshots(projectId)

// 获取单个快照详情（含完整BOM）
getProjectSnapshotDetail(snapshotId)

// 对比两个快照
compareProjectSnapshots(snapshotId1, snapshotId2)
```

### 步骤3：修改Projects.tsx

**添加快照历史Tab**：
- 快照列表表格
- 保存快照按钮
- 查看快照详情
- 对比快照功能

---

创建时间：2026-06-22
需求来源：用户反馈
设计状态：待实现