# CostHub 物料成本洞察模块 · 完整需求文档 v3

> **本文档为最终整合版，供直接提交给 Claude Code 使用。**
> 此前散落的多份补充文档（拆解终止条件、分解与趋势洞察联动、洞察入口与层级汇总Skill）中的内容已全部吸收进本文档并做了修订，**请以本文档为准**，此前文档仅作背景参考，如有冲突以本文档为准。
> 建议开一个新的 Claude Code 会话，把本文档完整提交，避免旧会话上下文里残留的早期不完整需求造成混淆。

---

## 0. 现状问题清单（本次修订要解决的具体问题）

1. **Bug**：API配置里"默认DeepSeek"与"资源池"是两套独立逻辑，资源池填了配置也无法生效，必须先填默认DeepSeek才行——这是硬编码默认供应商与新资源池机制没有统一导致的
2. 物料分解确认后，节点上**没有出现"洞察行情"按钮**，分解和洞察依然是脱节的
3. 树状图组件**完全空白**，需排查是数据未写入还是前端渲染问题
4. 树状图目前是**页面内嵌的一小块区域**，不是独立窗口，无法承载大规模节点和流畅的拖拽缩放交互
5. 洞察结果中的"关键事件"混在摘要文字里，无法结构化查看和按时间检索
6. 无法回溯查看某个历史时间点（如"5月份"）的完整查询快照
7. 预置的供应商提示信息不够多，希望扩充到10个以上并附免费额度说明和注册链接
8. 洞察结果中的来源链接目前不可点击，希望一键用系统默认浏览器打开

---

## 1. Bug 修复：统一 API 供应商配置

**问题根因**：代码中存在一个独立于资源池之外的、硬编码的"默认DeepSeek"分支逻辑，与后续新增的资源池机制未打通，导致两者互相依赖又互相冲突。

**修复要求**：
- **不允许存在任何游离于统一供应商列表之外的硬编码默认供应商**。DeepSeek 也只是 `api_providers` 表里的一条普通记录，不应有特殊分支代码
- 所有供应商（搜索类、大模型类）统一通过同一张表管理，通过 `is_active` / `priority` 字段决定启用与调用顺序
- 应用首次启动、供应商列表为空时，允许预置一条"示例配置"帮助用户理解格式，但**不能让这条示例配置在未填写真实Key的情况下参与实际调用**，需有明确的"未配置/不可用"状态区分，避免用户误以为已经能用

**数据结构（沿用此前设计，重申）**：

```sql
CREATE TABLE api_providers (
    id INTEGER PRIMARY KEY,
    provider_type TEXT NOT NULL,     -- 'search' 或 'llm'
    provider_name TEXT NOT NULL,
    api_key TEXT,                    -- 加密存储；未填写时为空，此条记录标记为不可用
    base_url TEXT,
    model_name TEXT,
    is_active BOOLEAN DEFAULT 0,
    priority INTEGER,
    is_preset BOOLEAN DEFAULT 0,     -- 是否为系统预置的示例/推荐条目
    monthly_quota_note TEXT,
    registration_url TEXT,           -- 注册/控制台链接
    created_at DATETIME
);
```

---

## 2. 预置供应商列表（扩充至10个以上）

系统内置一份"推荐供应商"参考列表（`is_preset = 1`），用户可以基于此列表一键"添加为我的配置"（填入自己申请的Key），而不是从零开始查资料。每条预置记录需包含：供应商名称、类型、免费额度大致说明（注明"具体以官网为准，可能变动"）、注册链接。

**要求**：Claude Code 在实现时，需**自行核实并更新以下链接和额度信息的有效性**（本文档整理时的信息可能已过期），并尽量补充到10个以上、覆盖国内外主流的开发者友好型选项。参考方向：

搜索类：
- Tavily（面向AI应用的搜索API，通常有较高免费额度）
- Bocha 博查（国内搜索API，中文场景效果较好）
- Serper（免费试用额度+低价按量计费）
- 其他类似面向开发者的搜索API聚合服务

大模型类：
- DeepSeek 官方API
- 智谱GLM（部分模型有面向个人开发者的免费额度）
- 月之暗面 Kimi
- 硅基流动 SiliconFlow（聚合多个开源模型的网关，多为免费/低价）
- 阿里云通义千问、腾讯混元等国内厂商的个人开发者免费档
- 火山引擎/ 腾讯云的"DeepSeek+联网搜索"打包接入点（可作为搜索+LLM二合一的便捷选项单独列出）

**注意**：不要在代码里写死"这个一定免费"这类断言，免费额度政策变化频繁，界面上统一用"以官网为准"这类措辞处理。

---

## 3. 物料分解：节点类型与终止条件

### 3.1 节点类型

新增字段区分结构层级节点与终端物料节点：

```sql
ALTER TABLE component_decomposition ADD COLUMN node_type TEXT NOT NULL DEFAULT 'structural';
-- 'structural'：仅组织层级，可继续拆解，不挂趋势监控
-- 'terminal'：拆解到此为止，是实际可分析、可采购的最小颗粒度，需关联趋势监控

ALTER TABLE component_decomposition ADD COLUMN ai_suggested_node_type TEXT;
-- 记录AI草稿阶段的原始建议，供确认时参考，用户可修改
```

### 3.2 判断终端节点的标准（满足任一条即可）

1. 该节点是实际业务中**可独立采购、有独立报价**的计价单元（即使理论上还能再拆，但实际不会拆开单独采购）
2. 该节点已能对应到**有公开行情的原材料/大宗商品类别**（如"玻璃基板"→"光学玻璃"），再拆不会带来更多可用市场信号
3. 再往下拆，**公开信息查不到任何市场信号**（过度定制化的工艺细节）

不同分支允许拆解到不同深度，不强求整棵树层级对齐。

### 3.3 AI拆解时需同步给出类型建议

每次"AI拆解子件"，对每个子节点，AI需同步给出 `structural`/`terminal` 的建议，用户在"确认草稿"环节可修改。若修改与AI建议不同，界面小字提示"与AI建议不同"，不阻塞操作。

---

## 4. 节点操作入口：拆解与洞察并列、互斥

| 按钮 | 适用节点类型 | 作用 |
|---|---|---|
| ⚡ AI 拆解子件 | `structural` | 向下拆出子节点 |
| 🔍 AI 洞察行情 | `terminal` | 对该节点发起市场趋势查询 |

- 节点确认 `node_type` 前，两个按钮均不显示
- 确认后根据类型只显示对应的一个按钮，两者互斥，不同时出现
- **这是此前版本遗漏、必须实现的核心交互，请重点验证**

**批量操作**：
- 批量拆解：多选 `structural` 节点，一键批量执行AI拆解
- 批量洞察：多选 `terminal` 节点，一键批量发起洞察查询，单个失败不阻塞整批（重试1-2次后跳过并记录失败项，不中断整体流程）

**终端节点确认后自动关联趋势条目**：节点确认为 `terminal` 时，系统自动按 `query_category` 匹配或新建 `trend_items` 记录并写入 `trend_item_id`，不需要用户手动从下拉框选择；同一原材料类别被多个不同节点关联时应指向同一条 `trend_item`，不重复创建。

---

## 5. 洞察查询结果：结构化关键事件 + 历史快照回溯

### 5.1 问题

早期设计把"判断依据"作为一段摘要文字整体存储，导致：① "关键事件"无法独立检索和按时间定位；② 无法回溯查看某个历史时间点的完整判断画面。

### 5.2 数据结构调整

```sql
-- 每次查询的历史快照（只增不改）
CREATE TABLE trend_snapshots (
    id INTEGER PRIMARY KEY,
    trend_item_id INTEGER NOT NULL,
    query_time DATETIME,
    direction TEXT,              -- 上涨/下降/震荡/信号不明确
    confidence_level TEXT,       -- 高/中/低
    magnitude_min REAL,
    magnitude_max REAL,
    magnitude_reference TEXT,    -- 幅度对应的时间基准，如"累计年初至今"/"环比上月"
    overall_summary TEXT,        -- 整体一句话摘要（用于列表页/悬浮卡展示）
    suggested_action TEXT,
    source_type TEXT NOT NULL DEFAULT 'direct_query',  -- 'direct_query' / 'aggregated'
    FOREIGN KEY (trend_item_id) REFERENCES trend_items(id)
);

-- 关键事件：从查询结果中结构化提取，独立于摘要文字存在
CREATE TABLE trend_key_events (
    id INTEGER PRIMARY KEY,
    trend_snapshot_id INTEGER NOT NULL,
    event_date DATE,                     -- 事件发生日期（若原文只有模糊时间如"今年以来"，允许为空或存粗粒度月份）
    event_description TEXT NOT NULL,     -- 事件描述，如"某厂宣布对XX制程涨价10%"
    impact_direction TEXT,               -- 该事件对趋势的影响方向：利多上涨/利多下跌/中性
    source_title TEXT,
    source_url TEXT,
    FOREIGN KEY (trend_snapshot_id) REFERENCES trend_snapshots(id)
);
```

**查询流程调整**：大模型结构化输出时，除方向/置信度/幅度外，需**单独提取一份关键事件列表**（每条含日期、描述、影响方向、来源），而不是把事件揉进一段摘要里。若原文信息无法拆出明确的独立事件，允许事件列表为空，`overall_summary` 仍需提供整体判断依据的简述。

### 5.3 历史时间点回溯

节点详情面板新增**时间轴选择器**：展示该物料所有历史查询的时间点（对应 `trend_snapshots` 的 `query_time`），用户点选某个历史时间点后，详情面板整体切换为**那一次查询的完整快照**——包括当时的方向、置信度、关键事件列表、来源，而不仅仅是趋势图上的一个数值点。默认展示最新一次快照，用户可随时切换回顾任意历史时间点。

---

## 6. 层级趋势汇总（Rollup）

沿用此前设计的核心机制，本节重申并与本文档其他部分对齐：

### 6.1 触发条件

`structural` 节点的直接子节点中，已有足够比例完成洞察查询后，出现"汇总本级趋势"按钮。允许部分子节点未完成时先看草稿版汇总，需明确提示"基于 N/M 个子节点，可能不完整"。

### 6.2 汇总方式：规则计算 + AI综合

- **规则层**：以 `cost_ratio_estimate` 为权重，计算子节点方向的加权得分作为参考；"信号不明确"子节点占比过高（如超40%）时自动下调汇总置信度
- **AI综合层**：基于子节点的方向、幅度、关键事件，识别关键驱动因素，生成叙述性汇总结论，而非简单罗列。汇总结论本身也应产出对应的 `trend_key_events`（可以是"继承"自权重最高的子节点的关键事件，也可以是AI综合后识别出的新的驱动性事件）
- 汇总结果存入 `trend_snapshots`，`source_type = 'aggregated'`，并通过 `rollup_contributions` 表记录具体基于了哪些子节点及其权重（结构沿用此前设计，见下）

```sql
CREATE TABLE rollup_contributions (
    id INTEGER PRIMARY KEY,
    parent_snapshot_id INTEGER NOT NULL,
    child_component_id INTEGER NOT NULL,
    cost_ratio_used REAL,
    direction_used TEXT,
    FOREIGN KEY (parent_snapshot_id) REFERENCES trend_snapshots(id),
    FOREIGN KEY (child_component_id) REFERENCES component_decomposition(id)
);
```

### 6.3 递归向上

子节点的汇总结果（`aggregated`）可作为更上一级汇总的输入，与终端节点的直接查询结果（`direct_query`）等价对待，支持多层级递归汇总。

---

## 7. 分析清单进化机制（替代早期"自动黑箱学习"设想）

### 7.1 设计定位

**用户明确倾向**：不是让AI在后台悄悄调整回答方式（黑箱式"学习"），而是维护一份**用户可见、可查看、可手动增删的"分析清单/Checklist"**，随着使用逐渐被充实，相当于把用户的分析习惯和关注重点显式沉淀下来。

### 7.2 触发与运作方式

- 该清单**贯穿所有追问场景**——不论是终端节点的洞察追问，还是层级汇总的追问，只要用户多次针对某一类角度追问（如反复问"预计持续多久""是否有替代供应商"），系统记录这一追问模式
- 达到一定重复次数（如同类追问出现3次以上）后，系统**建议**将该角度添加进"分析清单"，由用户确认是否采纳（不自动添加，需人工确认）
- 清单一旦被采纳某一条，之后每次对新物料发起洞察查询时，该条目会**主动**作为查询提示的一部分，让AI首次回答就尽量覆盖这个角度，而不必用户每次重新追问
- 用户可随时打开"分析清单"页面查看当前所有条目，手动新增、编辑、删除，不依赖系统自动触发

### 7.3 数据结构

```sql
CREATE TABLE analysis_checklist (
    id INTEGER PRIMARY KEY,
    item_description TEXT NOT NULL,      -- 如"预计涨跌持续时间"
    is_active BOOLEAN DEFAULT 1,         -- 是否启用（纳入查询提示）
    source TEXT,                          -- 'user_added' 或 'suggested_from_pattern'
    created_at DATETIME,
    updated_at DATETIME
);

-- 追问模式识别的原始记录，用于生成"建议加入清单"的提示（不自动执行）
CREATE TABLE followup_pattern_log (
    id INTEGER PRIMARY KEY,
    question_theme TEXT,                 -- 对追问内容的粗粒度归类
    trend_item_id INTEGER,
    created_at DATETIME,
    FOREIGN KEY (trend_item_id) REFERENCES trend_items(id)
);
```

### 7.4 与Rollup模块的关系

此前设计中曾把"进化"机制单独绑定在层级汇总的用户修正场景（`rollup_feedback`表），现改为：**汇总场景的用户修正，也作为一种"追问/反馈模式"汇入本节统一的分析清单机制**，不再单独维护一套割裂的进化逻辑，避免系统内出现两套不同的"学习"设计。`rollup_feedback` 表可保留用于记录具体修正内容，但其对系统行为的影响统一通过"生成清单建议"这一条路径体现，不做自动化的规则参数调整。

---

## 8. 树状图：独立窗口架构

### 8.1 为什么必须是独立窗口

树状图一旦拆解超过两三层，节点数量会快速增长，需要接近整个屏幕的画布空间才能支撑"看清楚"和"流畅拖拽缩放"。页面内嵌的一小块区域从物理空间上就无法承载这些交互设计，这不是审美选择，是空间约束决定的。

### 8.2 具体实现要求

- 使用 Tauri 的多窗口能力（`WebviewWindow` / 对应的 `tauri::window` API），"物料洞察"是一个**真正独立的操作系统级窗口**，不是页面内嵌组件，也不是 Modal 弹窗
- 默认打开时占满或接近占满屏幕，支持拖到副屏、自由调整大小
- 主窗口（仪表盘/器件库/BOM）中的"物料洞察"图标点击后，**唤起或聚焦**该窗口：若已打开则切换焦点并通过窗口间事件（`emit`/`listen`）传递"定位到哪个节点"的信息；若未打开则新建窗口
- 关闭洞察窗口后，主窗口需正确感知窗口已关闭的状态，避免下次点击时引用已销毁的窗口对象
- 两个窗口可能同时读写 SQLite，需确认后端数据访问层统一管理连接，避免锁冲突

### 8.3 树状图内部交互（沿用此前设计要点）

- 自由拖拽平移、以鼠标位置为中心的缩放
- 节点展开/收起有过渡动画（生长+淡入），布局重排平滑过渡
- 节点悬浮显示信息卡，卡片弹出位置需做边界检测避免超出屏幕
- 大树优化：搜索定位框、默认折叠深度（如展开到第二层）、小地图缩略视图
- 节点文字过长时用省略号提示截断，不做无提示硬截断
- 技术选型：优先用现有 ECharts 树图实现并配置动画；若效果不理想，评估引入 d3（现有技术栈已包含）做深度定制
- 独立的"关注清单"仪表盘视图：将所有挂载趋势监控的节点按更新时间/变动幅度平铺排序展示，作为日常首屏视图，树状图保留给深度梳理场景

---

## 9. 交互细节：来源链接可点击

洞察结果、关键事件列表中的所有来源链接，需支持点击后通过**系统默认浏览器**打开，而非应用内嵌浏览器视图。

Tauri 中直接使用官方提供的 `opener` 插件（或 `shell.open()` API，视 Tauri 版本而定）实现，无需自行开发。要求 Claude Code 在实现前**核实当前所用 Tauri 版本对应的正确 API 名称**（该能力在不同 Tauri 版本间有过命名调整）。

---

## 10. 排查建议：当前树状图空白问题

在按本文档重新实现前，建议先定位现有空白问题的根因，避免带着旧bug往前叠加新功能：

1. 确认"确认草稿"操作是否真的把数据写入了 `component_decomposition` 表（可用SQLite查看工具直接检查表内容）
2. 若数据库有数据但界面空白，检查前端树组件接收到的 props/数据格式是否符合 ECharts tree 组件要求的嵌套 `children` 结构（后端若返回扁平列表，需要前端做转换，这是常见遗漏点）
3. 检查浏览器/Webview 开发者工具是否有渲染报错

---

## 11. 分期建议（在原v2基础上调整）

考虑到当前已经出现"需求碎片化导致实现偏差"的问题，本次建议**先完整替换/修复现有实现的核心问题，再叠加新功能**，具体顺序：

### 第一步：修复现状问题（优先级最高）
- Bug：统一API供应商配置逻辑
- 补齐"洞察"按钮（与拆解互斥）
- 排查并修复树状图空白问题（先在现有嵌入式区域内修复数据链路，验证通了再迁移到独立窗口）

### 第二步：结构与数据层
- 节点类型（`node_type`）与终止条件判断
- 关键事件结构化存储
- 历史快照与时间轴回溯

### 第三步：独立窗口与树状图完整体验
- 迁移为 Tauri 独立窗口
- 拖拽缩放动画、大树优化、关注清单视图

### 第四步：进阶能力
- 层级趋势汇总（Rollup）
- 分析清单进化机制
- 供应商预置列表扩充、来源链接可点击（这两项工作量小，可穿插在任意阶段顺手完成）

**每完成一步，建议手动验证一遍现有核心功能（仪表盘、器件库、BOM、竞品对比）未受影响，再继续下一步，避免功能范围扩张的同时引入新的回归问题。**
