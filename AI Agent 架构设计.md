# CostHub AI 能力演进设计（v1，2026-08-12）

> 讨论结论沉淀：用户（成本经理）确认的方向与设计细节。本文档是后续开发的依据，先读本文件再接续。
> ✅ 1（器件报价比对）已于 2026-08-12 实现（part_aliases/part_compare_cache + 模块头按钮 + 三层流程 + 指纹缓存 + 竞态防护），实现细节见 CLAUDE.md v2.3.19 条目。

## 0. 工具定位（用户反思后的共识）

**工具不与 Excel 拼操作效率（必输），只做 Excel 做不到的事**：
- 数据沉淀：跨项目/历史报价/供应商报价集中管理
- 自动分析：基于沉淀数据给出结论（Excel 做不到）
- 交互原则：录入靠 Excel（导入导出无缝）；常用操作 Excel 手感（点格即改、清0即删、自动算、不弹窗）

**信息架构方向（远期）**：12 导航项收敛为「项目 / 器件 / 趋势 / 设置」四区；项目为中心（卡片流 + 状态点）；AI 从"页面"变"能力"（建议卡直接出现，不聊天）。此部分尚未实现，仅方向。

## 1. 器件报价比对（第一优先级，设计已定稿）

### 1.1 要解决的问题

同物料跨项目报价不一致（"M270 面板 ¥450，M300 同规格 ¥520"）是成本经理需要的情报；难点是**各项目 BOM 里同一物料的名称/型号写法不一致**（①同器件不同供应商料号 ②名称口语化差异："面板/屏/screen" ③规格后缀差异："27寸2K面板" vs "面板"）。AI 做疑似识别，人工确认，确认结果沉淀。

### 1.2 范围（用户确认）

- **跨项目、同品类、全部项目**（同品类项目一般只有几个，无需项目多选）
- **按模块切片**：一次只处理一个模块（如"面板模块"），模块内通常 5-30 行，本地模型上下文完全装得下，不做全局扫描
- **SKU 之间不对比**（同项目内差异已由 SKU 对比表覆盖）

### 1.3 三层流程（AI 与人的分工）

```
第一层：规则直接分组（本地，免费，不用 AI）
  完全同名+同型号；大小写/空格/全半角归一后相同 → 直接归组

第二层：本地 AI 疑似识别（只处理第一层剩下的行）
  语义疑似同一物料但写法不同 → 本地模型输出疑似分组 + 相似度 + 理由
  ⚠️ AI 只建议，不认定

第三层：人工确认
  「确认同一器件」→ 沉淀别名映射（永久记住，下次导入/比对自动套用）
  「标记不同」→ 记录，AI 不再建议
```

### 1.4 数据模型（新增表）

```sql
part_aliases (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  module_name TEXT DEFAULT '',       -- 作用域：模块（可空=全局）
  alias_name TEXT NOT NULL,          -- 写法 A
  alias_model TEXT DEFAULT '',       -- 写法 A 的型号
  canonical_name TEXT NOT NULL,      -- 规范名（确认后归一到它）
  canonical_model TEXT DEFAULT '',   -- 规范型号
  main_category TEXT DEFAULT '',
  sub_category TEXT DEFAULT '',
  source TEXT DEFAULT 'user',        -- user_confirmed / marked_different
  created_at TEXT DEFAULT (datetime('now','localtime'))
)
```
- 匹配逻辑：写入/导入/比对时，先归一化（trim + 全角转半角 + 小写 + 去内部空格/横线），查 part_aliases 命中则归组到 canonical
- 确认同一：把组内各写法各插一条 alias → canonical；标记不同：插 source='marked_different' 记录，AI 不再建议

```sql
part_compare_cache (              -- AI 疑似识别结果缓存（按模块指纹失效）
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  category TEXT DEFAULT '',       -- 品类作用域
  module_name TEXT NOT NULL,      -- 模块
  fingerprint TEXT NOT NULL,      -- 识别时该模块全部报价行的指纹（排序拼接 项目|器件名|型号|单价|数量）
  result_json TEXT DEFAULT '',    -- AI 疑似分组结果
  identified_at TEXT DEFAULT (datetime('now','localtime')),
  UNIQUE(category, module_name)
)
```

### 1.5 识别缓存与失效（用户确认：模块没变动不重复识别）

- **指纹失效机制**：打开比对弹窗时，先算该模块当前报价行指纹（同品类全部项目，行按 项目|器件名|型号|单价|数量 排序拼接）→ 与缓存指纹相同 → **直接用缓存结果（不跑 AI，秒开）**；指纹不同（器件成本变动 / 新增项目 / 模块有更新）→ 自动后台重新识别并更新缓存
- 指纹不变但用户确认了别名 → 不影响缓存；已确认的行下次由规则层（part_aliases）直接归组，不再进疑似组
- 弹窗可显示"上次识别时间"，手动"重新识别"按钮兜底

### 1.6 交互（入口嵌现有工作流，不单独建页）

- 项目管理页 BOM 清单按模块分组 → **每个模块头加「跨项目比对」按钮**
- 点击弹出该模块跨项目报价对比弹窗：
  - 规则组（直接分组，无争议，立即显示）
  - **AI 疑似组：后台自动运行**，识别期间弹窗顶部显示**工作状态提示（小机器人图标呼吸闪烁动画 + "AI 正在识别疑似物料…（N 行待识别）"）**——动效优先于文字，一眼看出"在工作"（⚠️ 低特效模式下 animation 被禁，降级为静态图标 + 文字）；完成/失败都有明确反馈（失败可重试）
  - 已确认组（含之前沉淀的别名，价差自动算，差异大标红 ▲）
- 组内展示：项目代号 / 器件名 / 型号 / 单价 / 导入时间，组底显示价差与异常标记

### 1.6 实现要点

- 规则归一函数 normalizePartName(name)：trim、全角→半角、lower、去空格/连字符（"DRV-100" vs "DRV 100"）
- AI 疑似识别 prompt：一次给一个模块内未分组行（名称+型号+所属项目），要求输出疑似分组 JSON（[{group, parts:[...], reason, similarity}]），本地模型（Ollama native + format:json + think:false）
- **自动触发**：打开比对弹窗即自动启动 AI 识别（规则分组先显示）；弹窗内 loading 提示"AI 正在识别疑似物料…（N 行待识别）"，完成/失败都有明确反馈（失败可重试）
- 复用 db 现有通道；不新增云端调用（全程本地，符合数据安全第一原则）
- 确认结果写入 part_aliases（status 区分 user_confirmed / marked_different）；getParts 匹配复用逻辑接入 alias（后续导入 BOM 时自动归组）

## 2. 后续路线（未启动，方向已讨论）

### 2.1 规格级项目预估（用户工作流：拿最像的旧项目改规格估差异）
- 项目规格对比表：行=规格维度（尺寸/分辨率/刷新率/面板/接口…），列=历史项目，规格差异旁显示对应成本差异（从模块成本反推）
- 选新项目规格 → 匹配最接近的历史项目 → 估算成本 + 差异依据

### 2.2 界面轻量化（信息架构收敛）
- 12 导航 → 项目/器件/趋势/设置
- 项目工作台：7 tab → 1 屏（左成本构成右器件表，SKU/对比/历史为展开区）
- 项目卡片流 + 状态点（🔴 待谈价/异常）

### 2.3 谈价提醒（用户评估：趋势是常识，价值有限 → 降级为可选）
- 用户观点：屏/存储等趋势成本经理日常工作有敏感性，AI 提醒"鸡肋"
- 保留价值点：BOM 快照 + 脱敏关键词 + 云端查趋势的链路（本地筛选→脱敏→云端）可作为能力储备，不做主动推送

## 3. 开发顺序建议

1. 器件报价比对（1 已定稿）—— 先做
2. 界面轻量化（2.2）—— 交互收益最大，配合报价比对入口
3. 规格级预估（2.1）—— 需要设计规格→成本映射
4. 谈价提醒 —— 暂缓
