## 最新状态（2026-08-16：演示稿 19 页收尾 + 供应商历史按钮修复，已推送）

- **上一轮（第 19 轮）**：演示文稿补 P14 aiBridge 页（18→19 页）+ 讲稿同步；修复供应商管理页两个详细列表历史按钮失效 bug（ODM/器件列表）；清理调试残留；tsc -b 0 错 + 96 vitest 全过，已推送
- **构建由 AI 负责（2026-08-16 起约定）**：代码改动完成后由 AI 执行 `npm run build` + `npm run tauri:build` 产出最新 exe（构建前确认 costhub.exe 未运行；build.bat 末尾有 pause 不适合脚本环境，故分步执行）；验证产物时间戳后向用户确认
- 演示数据已就绪（seed_full_demo.py 已跑过，db 无需再动）
- 演示数据已就绪（seed_full_demo.py 已跑过，db 无需再动）
- **试用后反馈给 AI**：驾驶舱目标预警 / 项目状态点 / 项目体检 / 快照 AI 解释 / 规格预估 / 全局问询 的体验问题与改进意见
- **下一步待讨论（等用户试用反馈确认方向）**：
  1. 项目工作台 1 屏化（7 tab 合成一屏，Projects.tsx 大手术）
  2. 谈价提醒 / 洞察知识库（规划 2.3 + 演示稿近期计划）
  3. 价格异动预警的进一步强化
  4. 云端搜索 provider 是否补 key（当前靠 DeepSeek 原生搜索兜底）
- ⚠️ 若对话上下文将满：新对话先让 AI 读本文件 + CLAUDE.md 即可无缝接续

## 当前进度（2026-08-14 会话：AI 能力嵌入工作流 + 工程优化）

### 工程优化（已完成，已推送 GitHub costhub）
- 统一版本号 2.3.19；删除废弃页面（TrendInsight/AnalysisChecklist/CostControl）；.gitignore 完善
- 性能：页面级 React.lazy（主包 4.2MB→302KB）+ ECharts 按需注册（src/echartsSetup.ts）+ 虚拟滚动
- 可维护性：**db.ts 按业务域拆分为 src/db/ 11 文件**（core/auth/parts/suppliers/projects/competitors/compare/trend/settings/worklog/dashboard），db.ts 仅聚合导出；生成工具 _tools/split-db.mjs
- 可靠性：vitest 测试框架（30 用例：SKU 成本/normalizePartName/目标达成/项目体检）+ 16 个高频查询索引

### AI 能力真实落地（用户核心诉求：AI 要真实有效用起来）
- **仪表盘 → 驾驶舱**（Dashboard.tsx + src/targetInsight.ts）：目标成本达成预警置顶（未达标项目/领域红色卡片，点击直达项目页）+ AI 今日洞察区（报价情报/成本快照异动）+ 跨页直达事件 costhub-open-project
- **项目页 AI 体检条**（Projects.tsx + src/projectHealth.ts）：规则自动检查 4 类问题（BOM 缺单价/数量0、目标超支、快照异动、同类报价偏高），点击直达对应 tab；「AI 小结」按钮用本地模型一句话概括（失败静默）
- **成本快照对比 AI 解释**：对比弹窗内「生成解释」——差异清单交本地模型流式生成原因说明
- **全局 AI 问询**（src/components/GlobalAI.tsx）：侧边栏入口，带当前项目上下文（localStorage costhub-ctx），回答可一键记入工作手账（带项目标签）；数据安全边界=只有本地模型读数据
- **首次 AI 能力引导**（src/components/AIGuide.tsx）：解锁后首次显示 5 项 AI 能力 + 配置就绪度，一键去设置
- **界面轻量化第一步**：导航收敛四区（驾驶舱 + 项目中心/数据资产/AI 趋势，页面零改动）+ 项目列表状态点（src/projectStatus.ts，目标超支红/情报与异动橙）——「项目工作台 1 屏化」待用户确认后推进
- **全功能演示数据**：seed_full_demo.py（幂等可重跑）——目标成本 15 / 供应商+历史 / 快照 33 / SKU / ODM / 规格全覆盖；附带快照异动按 created_at 排序修正
- **器件供应商价格趋势小结**（src/supplierTrend.ts）：器件库供应商抽屉自动显示每个供应商价格趋势（持续涨/降/波动/平稳 + 累计幅度 + 最近原因），表格趋势列 + footer 小结条——②嵌入点全部完成

### 用户现状配置（生产库）
- 云端：DeepSeek 官方激活（deepseek-v4-flash）；搜索 provider 全部未配 key（趋势洞察依赖 DeepSeek 原生搜索兜底）
- 本地：Ollama qwythos-9b:latest（报价情报/体检小结/全局问询/快照解释都靠它）
- ⚠️ 项目目标成本数据极少（仅 2 条）——驾驶舱预警依赖目标设定，建议用户先在「项目管理 → 成本分析」设定领域目标

### 待做（规划 AI Agent 架构设计.md 未启动项）
- ✅ 已交付：规格级项目预估（src/specEstimate.ts + 项目管理页弹窗）、界面轻量化第一步（导航四区 + 项目状态点）、器件供应商价格趋势小结（src/supplierTrend.ts）
- ⏳ 剩余：**项目工作台 1 屏化**（7 tab 合成一屏，Projects.tsx 大手术，需用户确认方向）、谈价提醒/洞察知识库（降级为能力储备）、价格异动预警强化

# CostHub 项目接续说明

> 本文件用于新开对话时快速接续上下文。**铁律：每次任务完成后立即更新本文件 + CLAUDE.md**（先改代码 → 再改文档 → 才算完成），不攒到会话结束。

## 当前进度（2026-08-13 会话）

### 方向讨论（设计文档已落盘：`AI Agent 架构设计.md`）
- **用户反思**：功能多但交互重，日常操作不如 Excel 便利；工具定位 = 不做 Excel 能做的事，只做数据沉淀 + 自动分析；交互原则 = Excel 手感（点格即改/清0即删/自动算/不弹窗），录入靠 Excel 导入导出
- **器件报价比对**（设计定稿，待开发）：同物料跨项目报价差异情报——三层流程（规则分组→本地 AI 疑似识别→人工确认沉淀别名映射 part_aliases 表）；范围=同品类全部项目、按模块切片（模块头「跨项目比对」按钮，嵌项目 BOM 清单）；AI 只建议不认定；SKU 间不比（SKU 对比表已覆盖）
- **后续路线**：规格级项目预估（拿最像旧项目改规格估差异）、界面轻量化（12 导航→4 区、项目工作台 1 屏化、项目卡片流）、谈价提醒降级（用户判断趋势是常识，价值有限，链路留作能力储备）

## 2026-08-12 会话：演示文稿重构（非代码改动）

- **CostHub演示文稿.html 重构为"AI 应用实践"分享叙事**（18 页）：封面改定位（COSTHUB × AI 应用实践，受众=成本/采购同行）→ 痛点点题（一半靠收拢一半靠 AI）→ 数据收拢是 AI 前提 → 全链路概览 → 器件库/模块库/项目/对比/手账/交接（数据基建部分）→ 双模型通道架构图（原样保留，用户明确要求）→「AI 在成本工作里做了什么」（原来→现在三卡）→「AI 物料洞察」（批量/留档/Skill 定制/拆解树/安全）→「AI 演示生成」（本地模型直接出文件 + 习惯库拆解应用）→「AI 的边界·它帮不了什么」（实事求是页）→ 配置教程两页保留 →「后续规划」（分级不画饼）+ 谢谢观看
- ⚠️ **「AI 怎么做到可信」页已按用户要求删除**（2026-08-12 当日）：原四道检查（反幻觉/时效/口径/冲突预警）+ 9 框架 pills 整页移除，页序 P14-P17 → P13-P16；9 框架仅保留 P4 概览统计条数字提及
- **示意增强（同日）**：P2 痛点页、P3 收拢页的两个 AI 点题 banner 按用户要求删除（AI 不抬位）；P5 器件库新增「供应商价格历史」示意表（时间/供应商/变动/原因，改价必填原因）；P6 模块库新增「模块成本基线·持续刷新」时间线示意（A001→A002→A003 当前基线）+ 核心能力加"基线随报价/定型持续刷新"；P7 项目管理页按用户四点重排六卡（BOM 清单一键导入 / 目标成本设定+降本措施跟踪 / 成本快照自动留痕随时对比 / 关键节点成本测算 Charter→ADCP→量产→持续降本 ¥120→105→98→92 / ODM / 定型），原「成本核算」「BOM 横向对比」卡移除（后者 P8 已覆盖）；**P8「对比分析」重构为「竞品分析·设计差异成本评估」**——三卡（竞品 BOM 估算与差异评估 / 独立竞品器件库不与我司混淆 / 特性×成本契合分析）+ 手写 SVG 五维雷达图示意（我方 indigo vs 竞品灰，性能/规格/显示/外观/可靠性）+ 成本对比契合判断卡（8.6 分 vs 7.4 分、¥1,180 vs ¥1,240），⚠️ 雷达图首版多边形顶点坐标算错（非正五边形，顶角两顶点近乎重合）→ 已用正五边形公式重算全部顶点（中心 210,135 R=95，标签/内框/中线同步修正，多边形数据点各加小圆点）；「核心场景：竞品对标」banner 按用户要求删除；⚠️ **雷达图再升级为六维「竞争力雷达·特性+成本」**（2026-08-12 当日）：新增"成本竞争力"维度（整机成本折算分，越低价越高分，语义与雷达图"越大越强"统一），正六边形公式重算全部顶点（中心 210,135 R=95，每 60°），卡片分数与雷达值自洽（特性分=五维均值 8.6/7.2，成本力分=成本折算 8.2/7.0，整机 ¥1,180/¥1,240），图例加"成本竞争力=整机成本折算分"说明；⚠️ **成本力数值按公式修正**（2026-08-12 当日）：演示稿原拟 8.2/7.0 与工具公式（10×组内最低BOM成本÷本产品BOM成本，封顶10）不符，按公式改为我方 10.0（¥1,180 组内最低）/竞品 9.5，雷达成本竞争力维度值 0.82/0.70 → 1.0/0.95（顶点坐标重算，顺带统一外观/可靠性顶点与卡片五维均值一致）；**特性成本分明细表**（用户指定口径：每个特性看对应的成本分，同一特性成本越低得分越高）：新增 diagram 表格，每特性一行（性能/规格/显示/外观/可靠性 + 整机），成本分 = 10×组内该特性最低成本÷本产品该特性成本（特性成本=关联模块成本合计），最低者标绿——整机我方更便宜但显示/可靠性两特性竞品更省，点题"特性级成本分把整机便宜掩盖的局部劣势翻出来"（后按用户要求**特性成本分表并入雷达+契合判断同一 diagram 框**：上排左雷达右判断、下排分隔线全宽表格）**成本长城示意**（同日追加）：手写 SVG 成本长城图（柱=规格差 +0.25/+0.15/+0.10/0.0/+0.20，曲线=成本差 −¥12/−¥8/−¥6/−¥15/−¥5，双轴 0 居中），按用户要求单独一行全宽展示（左 SVG + 右"怎么读这张图"四组合判读条），成本对比契合判断卡回到雷达图右侧一起看（后按用户要求**雷达图与契合判断合并进同一 diagram 框**，左雷达右判断配套看）；⚠️ 插页时多写了一个 two-col 关闭 div 导致标签不匹配，已修复（组装后 html.parser 全绿）；**新增 P13「AI 物料洞察·行情研判有据可查」**（P12 之后）——三卡（批量洞察有界并发 / 洞察历史留档按时间线回看 / 内置 9 Skill + 可定制自己的 Skill）+ 物料拆解树状图示意（27寸 BOM：面板模块 62%→27寸 2K 面板 52% 关键物料，monospace 树）+ 洞察链路数据安全卡（云端 LLM 无数据库权限 / 每次外传提示词全程记录可供审查 / 请求发出前预览确认）；**新增 P14「AI 演示生成·本地模型直接出文件」**（P13 之后）——四步流程条（粘贴素材→勾选习惯→本地模型生成→直接出文件）+ 三卡（直接产出 HTML/PPTX 文件 / 习惯库拆解以往 PPT 大纲沉淀结构·风格·素材模板并应用 / 全程本地无云端通道）+ 原来→现在对比（一版一下午→几十秒一版），对应工具 DemoGenerator.tsx + pptxExtract.ts 实际能力；后续页顺移：边界 15、教程 16/17、规划+结尾 18，共 18 页
- 删除：原「细节打磨」页；原「数据管理安全」页并入 AI 架构页（登录门禁/审计日志/Key 加密三条 flow-row）
- 素材：logo 三处共用同一 base64（47KB），新稿用 __LOGO__ 占位符 + python 替换组装
- 原版备份：`CostHub演示文稿-原版备份.html`（项目根目录，git 未跟踪，审阅后确认无需可删）
- 讲稿口径：全部沿用工具实际能力表述，无夸大；模拟数据示例，未用真实业务数据
- **配套演讲逻辑文档**（同日）：`CostHub演示文稿-演讲逻辑.md`——17 页逐页讲解要点（一句话核心/讲什么/衔接话术）、章节时间表（数据基建 10-12 分钟、AI 主戏 12-15 分钟）、四个重点页（P9 手账/P11 架构图/P13 四道检查/P14 边界）、开场与结尾话术、Q&A 预判

## 当前进度（2026-08-08 会话）

### 最近完成的功能
1. **登录门禁**：用户名+密码登录（初始 admin/666666），密码错误进受限模式（数据全隐藏），双击 Logo 找回密码（界面不提示）
2. **设置页左导航重构**：Claude 风格（AI 服务/分析框架/安全设置/个性化/审计日志/关于），点击切换不滚动
3. **用户名设置**：设置页可改用户名，登录页双验证
4. **Token 用量统计**：外部 LLM 调用自动记录 token，设置页审计日志展示
5. **快捷洞察区**（物料趋势洞察页）：无需分解树直接洞察单物料（如"锂电池"），卡片显示最近洞察时间+成本趋势
6. **快捷洞察详情弹窗**（2026-08-05 完成）：点击卡片 → 分 Skill 差异化呈现 + 建议动作 + 历史批次记录 + 多 Skill 融合总结（多数方向规则）
7. **快捷洞察白屏修复**（2026-08-05）：renderSkillDimensions 再次因定义在清单页 return 之后导致白屏 → 移到清单页 return 之前；卡片操作按钮精简（去掉独立"详情"按钮，点击卡片即打开详情，避免按钮重叠）
8. **洞察时间时区修复**（2026-08-05）：`saveTrendSnapshot`/`saveQuickTrendItem`/关注物料创建 trend_item 原用 `toISOString()`（UTC，比北京时间少 8 小时）→ 改为本地时间字符串（db.ts 新增 localNow()，与 SQLite datetime('now','localtime') 一致）
9. **洞察时间显示修复（二次）**（2026-08-05）：根因是字符串排序错乱——旧数据 ISO 格式（带 T）与本地格式（带空格）混排，`'T'(0x54) > ' '(0x20)` 导致旧 UTC 时间永远排前面 → `getTrendSnapshots`/`getLatestTrendSnapshot` 改按 id DESC 排序；显示层新增 formatTime() 统一兼容两种格式
10. **Skill 时效性 + 反幻觉强化**（2026-08-05）：全局 prompt 升级"数据时效性要求"（时效分级 3/6 个月 + 置信度上限联动 + 禁模糊时间词）+ 新增"反幻觉硬约束"（只许用来源数据、禁训练知识补数字、禁编造来源、不确定就信号不明确）；9 个 Skill 各加专属"时效性规则"段
11. **快捷洞察详情弹窗重构**（2026-08-05）：新增顶部"历史洞察时间轴"（横向批次节点：MM-DD HH:mm + 方向 + 框架数，点击切换批次，节点按批次融合方向着色）+ 选中批次融合总结 + 分 Skill 差异化卡片（替代原"最近一批+文本历史列表"）
12. **详情弹窗增强**（2026-08-05）：时间轴选中节点可**删除该批次洞察**（deleteTrendSnapshot 连带维度/关键事件）；底部新增**基于洞察结论追问**（复用 agentSearchLoop + askLLM，携带最新快照上下文 + 历史对话，展示 Q&A 列表）
13. **DeepSeek 原生联网搜索接入**（2026-08-05）：官方 API 已原生支持 web_search（Responses API 声明 tools=web_search）——已实现：trendService 新增 tryDeepSeekNativeInsight（一轮调用完成搜索+分析，返回真实来源，含口径归一化校验），agentSearchLoop 优先走原生搜索（失败自动回退 Serper 流程）；设置页 AI 服务区新增"模型原生搜索"开关（settings 表 ai_native_search，默认开启）
14. **AI 总结提示词重构**（2026-08-06）：输出曾是"日期+做了什么+贡献"编号列表模板——重写两步提示词：①压缩阶段按时间排序、同项目串时间线、公共/其他条目标注【公共事务/能力建设/协作互助】性质；②正式总结加✅正确示范/❌错误示范、硬性禁止编号列表/逐条罗列/"贡献："模板句式，要求每项目写成"起因→动作→结果"连贯段落
15. **AI 总结语言正式化**（2026-08-06）：新增"语言风格（必须遵守，书面正式）"——禁止口语化（搞/弄/整/搞定/咱们等），提供正式替代动词（推进/主导/落实/达成），数字规范表述
16. **搜索开关失效 bug 修复**（2026-08-06）：`getActiveProviders` 过滤条件漏了 `is_active`（停用供应商仍被当活跃用）→ 已补上；`testSearchConnection` 去掉"停用也测第一个"的降级逻辑，全部停用时明确报错；resolveLLMConfig/resolveSearchConfig 同步清理
17. **洞察时间轴方向修复**（2026-08-06）：快捷洞察详情的时间轴节点原按 id DESC（最新在左）→ 改为 timelineKeys 反转（从左到右时间递增，最早在左、最近在右），默认选中仍是最新批次
18. **项目管理 ODM 整机供应商报价**（2026-08-06）：发现 `getProjectSuppliers` 原错误查询 part_suppliers（器件供应商表）→ 重写为查询 project_suppliers 表（整机报价）；新增 saveProjectSupplier/deleteProjectSupplier/getProjectSupplierPriceHistory/saveProjectSupplierPriceHistory；项目管理页新增"🏭 整机供应商（ODM）"tab（加权报价统计卡 + 供应商列表 + 添加/编辑弹窗，改价强制填原因记录历史 + 报价历史查看）
19. **PPT 产品介绍**（2026-08-06）：CostHub产品介绍.pptx（15 页，深墨蓝+金色品牌风格）——封面/背景痛点/平台概览/器件库/项目管理/物料洞察（快捷+分解树+详情弹窗）/AI 9 套框架/质量保障（时效+反幻觉+口径）/数据安全/设置中心/工作手账+AI总结/技术架构/供应商管理/建设历程/结尾；生成脚本 gen_ppt.cjs 在 outputs（pptxgenjs 环境），重新生成需在 outputs 目录运行
20. **HTML 幻灯片版演示**（2026-08-06）：CostHub产品介绍-幻灯片.html（15 页，深墨蓝+金色）——每屏一页、方向键/空格翻页、F 全屏、Esc 目录、触摸滑动、进度条、底部圆点跳转、元素入场动画（延迟分级）；内容覆盖全部功能含安全/AI/设置详解
21. **数据备份/恢复**（2026-08-06）：设置页新增"数据管理"分区——Rust 新增 4 命令（backup_database/list_backups/restore_database/delete_backup，备份存 exe 同目录 backups/，防路径穿越，恢复前自动备份当前库为 costhub-pre-restore-*.db，移除 WAL/SHM）；前端备份列表 + 立即备份/恢复/删除按钮；⚠️ 注意：备份用文件复制（未做 WAL checkpoint 合并，备份时若应用正在写入可能不完整——后续可加 PRAGMA wal_checkpoint 优化）；恢复需重启生效
22. **Excel 按内容类型导出**（2026-08-06）：设置页"数据管理"分区新增 Excel 导出卡片——6 类内容可选（器件库/项目/竞品/供应商/工作手账/洞察记录），每类独立 sheet 格式（器件含供应商报价、项目含 BOM 明细、供应商分器件/ODM 两 sheet）；前端 xlsx 库生成 → base64 → Rust save_export_file 存 exe 同目录 exports/（新增 list_exports/open_exports_dir 命令，Cargo 加 base64 依赖）
23. **项目导出按项目拆分**（2026-08-06）：项目导出改为"全部项目（每项目一个 sheet）/ 单个项目（下拉选择）"——每个项目 sheet 含三区：项目信息（A1 起）+ BOM 明细表（一行一个器件：模块/大类/子类/名称/型号/数量/单价/小计，带合计行）+ ODM 整机报价表；修复了原来 BOM 挤在一个单元格的问题
24. **HTTP 代理支持**（2026-08-06）：另一台电脑 LLM 测试报"Network request failed / error sending request for url"（HTTP 0）——网络层连接失败；Rust 新增 build_http_client（读 HTTPS_PROXY/HTTP_PROXY/ALL_PROXY 环境变量，reqwest Proxy::all），send_http 和 http_stream 共用；若代理环境变量没设置仍失败，可能是防火墙/杀毒/DNS 问题需在目标机器排查
25. **网络层恢复老版本（TLS 根因）**（2026-08-06）：根因是 reqwest 从 native-tls（默认，用 Windows 系统证书）改成了 rustls-tls（自带证书，不认公司根证书）——公司网络 SSL 拦截（governor 网关）环境下 TLS 握手失败；修复：Cargo.toml 恢复 `features = ["json", "stream"]`（native-tls）；build_http_client 去掉 30s connect_timeout、stream 不设总超时（与老版本 Client::new() 一致），仅保留环境变量代理支持。**已验证通过**（2026-08-06 另一台电脑 LLM 测试通了）
25b. **本地 Ollama 被代理拦截修复**（2026-08-06）：加代理支持后 `Proxy::all` 把 localhost:11434 也代理了 → 本地 AI 连不上；修复：`proxy.no_proxy(NoProxy)` 排除 localhost/127.0.0.1/::1（合并 NO_PROXY 环境变量）；⚠️ reqwest 0.12 的 `builder.no_proxy()` 是无参方法（读环境变量），要传 NoProxy 必须用 `proxy.no_proxy(np)`
26. **项目标签记忆 bug 修复**（2026-08-06）：AutoComplete 的 onChange 每次输入都触发"自动记忆"导致"鼠/鼠标/鼠标项"全被存进选项——改为只在**保存时**记忆（新增 rememberProject 函数，saveNote/addTodo 成功后调用），输入过程只更新状态
27. **模块库/器件库项目筛选**（2026-08-06）：模块库工具栏加"项目"下拉（选某项目只显示该项目下的模块分组），模块分组卡片加勾选框支持批量删除（checkedModIds，含半选状态）；器件库工具栏加"项目"下拉（前端按器件的 projects 字段过滤，filteredParts），配合已有行选择批量删除
28. **BOM 导入预览增强**（2026-08-06）：导入预览弹窗新增——①BOM 总价统计条（总价+条数+模块数，有异常时变橙色警告）②各模块小计标签（便于与原 Excel 逐模块核对）③小计列（单价×数量）④异常数据检测（缺名称/单价数量非数字/单价为0/数量为0/单价>10万/数量>1万），异常行红色高亮+⚠️提示+计数警告条
29. **BOM 数量支持小数**（2026-08-06）：分摊用量场景（如 0.000123）——导入解析 parseInt→parseFloat；SQLite INTEGER 列存小数会自动按 REAL 存（已验证，无需改表）；BOM 编辑弹窗/从器件库添加的 InputNumber 改 step=0.000001 min=0；数量显示智能格式化（整数原样、小数去尾零）；单价显示 toFixed(4)；新增"数量异常偏小(<0.000001)"检测
30. **虚拟模块过滤 + 项目专属模块**（2026-08-06）：①模块库排除空模块（成本预估阶段的占位模块，无器件，items.length===0 跳过；BOM 导入器件后自然成为正式模块）②项目筛选后只显示该项目专属模块实例（按项目+模块名精确过滤，同名模块跨项目分开显示，卡片带项目代号标签）③模块库/器件库编辑弹窗子类选项改为动态生成（数据库实际子类+常量表兜底，不再硬编码）
31. **筛选选项全部动态化**（2026-08-06）：①模块库分类筛选只用实际用到的分类（去掉硬编码默认值，getModuleCategories 无数据显示"未分类"）②品类筛选只显示实际模块用到的品类（usedProdCategories 从 modGroups 动态统计）③器件库项目筛选改为 **BOM 反查**（project_boms 关联的器件 ID，最准确）+ projects 字段匹配双保险（修复选项目后全空的问题——原匹配逻辑只用项目名，而 BOM 导入时 projects 存的是项目代号）
32. **模块库全选**（2026-08-06）：工具栏新增"全选 (N) / 取消全选"按钮——作用于当前筛选结果的所有模块实例，配合批量删除使用；已全选时按钮变"取消全选"
33. **模块详情操作列精简**（2026-08-06）：项目对比表操作列删掉多余的"分类"按钮（分类已在该表"分类"列显示，按钮与"编辑"功能重复），只留"查看/编辑"，列宽 100→120 防止溢出
7. **侧边栏增强**：底部用户区（头像+用户名）点击弹菜单（系统设置/使用说明/主题/缩放/版本）；使用说明用 HTML（public/guide.html）+ iframe
8. **用户自定义 logo**：蓝紫渐变风格，替换程序图标（8-bit PNG/ICO）+ 侧边栏 + 登录页
9. **筛选联动**：器件库/供应商管理"大类→子类"联动
10. **Skill 方法论体系**：9 个内置 Skill 专属方法论 + 洞察弹窗选 Skill + 差异化呈现（SWOT矩阵/PEST宫格/五力条/风险卡/韧性条）
11. **数字口径约束**：幅度=近1-3月价格变化，超±30%拦截，方向冲突提示
12. **洞察性能优化**：多 Skill 并行、搜索并行、批量有界并发、max_tokens 8000
13. **快照排序修复**：getTrendSnapshots 加 id DESC 兜底（同 query_time 批次取序稳定）



34. **成本计算舍入口径统一（2026-08-08）**：整机成本与 Excel 差几毛钱——根因是各页面累加时每行小计先 `Math.round(x*100)/100` 再累加（上一轮错误引入），与 Excel「每行小计舍入 2 位后相加」口径一致反而造成误差。已改：**中间计算一律用原始浮点值（该是多少就是多少），只有最终显示才 toFixed(2)**。修改位置：db.ts recordProjectCostSnapshot 的 bomCost、Projects.tsx bomTotal/modTotal/refTotal、ModuleLibrary.tsx total/expTotal、Reports.tsx total/total2/byCat（删掉 unused round2）。快照落库仍保留 4 位小数（bomCost*10000/10000，仅防浮点尾差）；项目快照变更对比阈值 0.01 不变。⚠️ 教训：**任何成本合计都禁止在累加循环内舍入**

35. **模块级成本显示 4 位小数（2026-08-08）**：模块成本是与供应商 Excel 精确核对的口径——Projects.tsx 模块小计/参考小计/差异（Tag + 表格汇总行）、BOM 导入预览（总价/单价/小计列/模块小计标签）、ModuleLibrary.tsx 模块成本列/明细单价/小计/展开模块成本/分类条形图标签 全部 toFixed(2)→toFixed(4)；整机总成本仍 2 位，分组卡片成本区间概览仍 toFixed(0)

36. **模块库 vs 项目页数据源统一（2026-08-08，结构性修复）**：模块库读 modules+module_items（导入时 Excel 快照），项目管理页读 project_boms JOIN parts（实时价）→ 两页偏差、项目总价错。修复：project_boms 固化快照列（part_name/part_model/part_cost/main_category/sub_category，ensureSchema 补列 + 一次性回填：优先 module_items 快照、无则 parts 兜底）；getProjectBOMs/recordProjectCostSnapshot/getSnapshotBOMDetail/getProjectModuleSummary/Dashboard 全部改「快照优先、parts 兜底」；addBOMItem 写入即固化快照；saveModuleItem/deleteModuleItem ↔ updateBOMItem/deleteBOMItem 双向同步（模块库改 ↔ 项目页改）

37. **BOM 导入器件错位修复（2026-08-08）**：另一台电脑真实数据导入后「同模块一个物料取代另一个物料，总数一致但金额/器件不对」——根因 doImport 匹配用 getParts(name)（LIKE %name% 模糊，命中"说明书"→"说明书 中文"等子串）+ find(仅比型号) 错误复用其他模块旧器件。修复：名称+型号必须完全相等才复用，否则新建（实测 MCU→新建而非误用 MCU STM32F103）；顺带修 projects 字段拼接误用 selectedPid（自动建项目时拼错）改为 targetProjCode

38. **BOM 导入数量为0允许导入但提示（2026-08-08）**：数量为0（占位/待定项）不再算红色异常——解析时拆分为 _issues（错误）/ _warns（警告）两级，数量为0与异常偏小(<0.000001) 归入 _warns；预览弹窗：异常列黄色显示警告、行不高亮红、顶部提示条单独显示「N 条数量为0（黄色提示，可正常导入）」；修复 saveModuleItem 的 `data.quantity || 1` 把 0 变 1 的 bug（改为 `?? 1`，0 为合法值）

39. **子类分布图"只有框架没有条形"修复（2026-08-12）**：模块库单模块子类分布图 + 器件级对比子类分布对比图只显示坐标轴/网格/标题，条形不渲染——根因 `barGradient(color)` 用 `color + '22'` 拼接底部淡化色，只对 hex 有效（`#4F46E5`+`'22'`=合法 8 位 hex）；而这两张图传的是 `rgba(79, 70, 229, 0.45)` 字符串，拼出 `rgba(79,70,229,0.45)22` **非法颜色** → ECharts 静默跳过条形。修复：chartTheme.ts barGradient 兼容 rgba()/rgb() 输入（解析出 alpha 减半作底部色，hex 路径不变）；ModuleLibrary 补 `barData[0].value || 1` 防全 0 时 NaN。⚠️ 教训：**给 ECharts 的渐变色 stop 颜色必须合法**（hex 8 位 / rgba 函数式），非法时只画框架不报错，排查优先检查颜色字符串

40. **子类分布对比图"条形颜色与图例不一致"修复（2026-08-12）**：器件级对比图的条形是自定义 Indigo 深浅（颜色写在**每个数据点** itemStyle 里），而图例颜色是 ECharts 按系列序号从默认调色板自动分配的 → 两边对不上。修复（按用户要求以图例为准）：颜色从数据点级移到**系列级** itemStyle（图例默认取系列色，自动匹配）+ 显式调色板 `cmpSeriesColors`（ECharts 默认 9 色）+ legend.data 每项显式同色双保险；条形仍走 barGradient 同色渐变。⚠️ 教训：**图例颜色取自系列级颜色，若颜色写在数据点级 itemStyle，图例不会跟随，必然不一致**——多系列图表颜色统一放系列级

41. **竞争力雷达六维图（2026-08-12，新功能）**：Compare 页顶部新增 `src/components/CompetitivenessRadar.tsx`（自包含组件）——六维雷达（性能/规格/显示/外观/可靠性 + 成本竞争力），我方项目 + 多竞品（同品类多选）叠加。要点：①product_features 加 type 列（custom/radar），ensureSchema 幂等种入五维种子；**旧对比雷达三处 getFeatures 调用全部改 getFeatures('custom')**，避免种子混入旧雷达 indicator ②评分存 product_scores（0-10 分制，与旧 0-100 按 feature_id 隔离），滑杆即时保存 ③成本力=10×(组内最低BOM成本÷本产品BOM成本) 保留 1 位封顶 10，单产品=10，缺失=null 不画；我方用 getProjectBOMs 快照累加、竞品用 competitors.bom_cost，禁混费率 ④**模块-特性关联**：新表 module_feature_links（module_name+feature_id 唯一，同名模块全局一致），主从式弹窗（左模块列表搜索+成本降序+已关联 Tag，右五维勾选即存；按特性检查视图防遗漏），评分弹窗每维列出关联模块小计/占比 Tag 作打分依据 ⑤验证：公式边界脚本全过（最低者=10/单产品=10/缺失=null/封顶）、雷达顶点 (0,100) 起每 60° 正六边形、tsc 通过。**待办**：build.bat 重新打包后人工验证雷达渲染与评分持久化；演示文稿 P8 雷达图与实际功能口径一致（示例 8.6/7.2、成本力 8.2/7.0 为模拟数据，非真实评分）

42. **竞争力雷达 build 报错修复（2026-08-12）**：用户跑 build.bat 报 3 个 TS 错误——①import 了未使用的 getScores（TS6133）②buildRadar push 的产品对象缺 scoreMap 属性（RadarProduct 类型必填，后置赋值不满足字面量检查）。修复：删未用 import + push 时补 `scoreMap: {}`（随后统一填充）。⚠️ 教训：**`npx tsc --noEmit | head` 的 `$?` 是 head 的退出码不是 tsc 的，会吞掉错误**——验证 tsc 必须重定向到文件再取退出码（`> log 2>&1; echo $?`）；本项目 Linux 沙箱跑不了 vite build（rolldown 需要 Windows 原生绑定），前端构建验证以用户 Windows 环境 build.bat 为准

43. **演示生成：本地 AI 产出 HTML/PPTX（2026-08-12，新功能）**：用户需求——Ollama 只能生成代码不能出文件，且案例素材保密只能本地处理；方案=**模型写内容（结构化 JSON）、本地模板/渲染器管样式**。已实现：①`src/components/DemoGenerator.tsx`（入口=本地 AI 助手左侧「📄 演示生成」按钮→1040px Modal，**完全不动现有聊天逻辑**）②工作流：选类型 HTML/PPTX → 手动贴素材 → 勾选习惯 → Ollama 流式 JSON（native+format:json+think:false+4000 tokens）→ parseDemoJson 容错（围栏/杂文本，失败重试一次）→ HTML 内置墨蓝金模板 / pptxgenjs 4.0.1 渲染 → save_export_file 落盘 exports/ ③习惯库=local_ai_context 复用 + **category 列**（ensureSchema 幂等；saveContextEntry 加参数）④**从成品提炼**：导入 .pptx → `src/pptxExtract.ts` jszip 解包（sldIdLst 页序 + rels 映射 + <a:t> 抽文本，round-trip 验证过）→ 大纲预览 → 存为"演示结构"习惯 ⑤代码整理（纯搬移）：startOllamaStream → src/ollama.ts；getSetting/setSetting/loadContextEntries 等 → db.ts 导出，避免循环依赖。⚠️ 沙箱 npm i 会触发 @tauri-apps/cli-win32 ENOTEMPTY——沙箱装依赖用手动 tarball 解压+手改 package.json，用户 Windows 上 npm install 即可（package.json 已加 pptxgenjs ^4.0.1）。**待办**：用户 Windows `npm install` + build.bat 后实测：生成 HTML/PPTX、导入 PPT 提取、习惯注入效果；模型输出 JSON 质量若差可调 temperature/num_predict

44. **竞争力雷达「特性 × 成本对比」（2026-08-12，功能完善）**：用户澄清核心诉求——**"同成本的特性比别人强还是弱"**（不只是评分对比）。实现：每个特性的成本 = 该产品中与此特性**关联的模块成本合计**（复用 module_feature_links + bomByModule，模块计入其所有关联维度）；**每分成本 = 特性成本 ÷ 特性评分**（同特性下谁低谁强）。UI：雷达卡片下方新增对比区块——每分成本分组条形图（X=特性、系列=产品、RADAR_COLORS 系列级颜色图例一致、越低越好）+ 明细表格（每产品列显示 评分·成本/每分成本，💪 绿色高亮组内每分最低者，独立"每分成本最低"列）；未配置关联橙色提示、未评分/成本0显示"—"。逻辑边界脚本验证过（结构件跨维计入、组内最强判定）；tsc 退出码 0。**注意口径**：竞品 BOM 若未按模块拆分（module_name 空）或模块名与关联配置不一致，特性成本计不进去——提示用户竞品侧也按模块录入

45. **成本长城图视图（2026-08-12，功能完善）**：用户提出——雷达图或成本长城图可选。长城图 = **柱（规格差：我方特性评分−竞品，正=规格强）+ 曲线（成本差：我方特性成本−竞品，负=成本低）** 组合图，判读"规格好且成本低 = 柱正+线负 = 💪优势特性"。实现：viewMode state 切换（雷达视图/成本长城图）；双 Y 轴各自 0 居中（规格差分 vs 成本差¥ 量纲分离）；多竞品每组柱+线同色（RADAR_COLORS 竞品从第二色起，与雷达一致）；柱带 ±数值标签；tooltip 按柱/线正负组合给结论；图下方四条判读说明。数据源全部复用已有（scoreMap + 特性成本 costTable）。**轴自适应（用户强调：示意优先，别跟实际极值否则图丑）**：adaptiveAxis 去极端（n≥8 去 2、n≥4 去 1、小样本全保留）→ 主体 ×1.25 → niceCeil（1/2/5×10ⁿ）→ 0 居中；极端超轴裁剪 tooltip 可读；验证：7 种分布下主体占轴 40-60% 全过。⚠️ 铁律已遵守：多系列图表颜色统一放系列级（柱/线都系列级 itemStyle）

46. **DemoGenerator JSX 引号 bug 修复（2026-08-12）**：`<Empty description="暂无习惯——把"导入 PPT"或"新建"作为第一步" />` 属性值里混入半角双引号导致 JSX 字符串提前闭合（TS1003/TS1382/TS1381 共 4 错）。修复：属性值改 JSX 表达式 `{'暂无习惯——把"导入 PPT"或"新建"作为第一步'}`。⚠️ 教训：**JSX 属性字符串里想放中文引号"..."时，属性值必须用表达式（{'...'}）或单引号包裹，否则半角引号会截断字符串**

47. **antd 类型缺失 269 错修复（2026-08-12，重大）**：build 报全项目 "Could not find a declaration file for module 'antd'"（TS7016）——根因：**antd 6.4.3 的 npm 包本身缺 .d.ts**（package.json typings 指向 es/index.d.ts 但文件不存在，es/ 目录 0 个 d.ts）→ tsc fallback 到 lib/index.js 报隐式 any，连锁 269 错（含所有 TS7006 隐式 any、DataTable 的 columns 报错）。修复：**升级 antd 6.4.3 → 6.6.0**（官方包类型齐全 1988 个 d.ts，已验证），package.json 固定 "6.6.0"（不戴 ^ 防再漂移）。另修 DemoGenerator 的 pptx.write API（4.0 改 `write({ outputType: 'base64' })`，字符串参数类型报错）和 4 个未用 import（Spin/Divider/FilePptOutlined/FileTextOutlined）、ollama.ts 未用 listen import。**⚠️ 重大教训**：**tsconfig.json 是 references-only（files:[]），验证必须用 `npx tsc -b`**——`tsc --noEmit` 检查空集合永远假通过，此前三次漏检全因此（竞争力雷达 3 错、JSX 引号、antd 269 错），全部是用户 build 才发现。从此验证一律 tsc -b 且重定向取真实退出码

48. **成本竞争力改为特性级（2026-08-12，用户要求）**：用户反馈"成本竞争力不该是总体的，而是每个特性看对应的成本分，同特性内成本越低得分越高"。实现：`featureCostScore`（每特性）= 10 × (组内该特性最低成本 ÷ 本产品该特性成本)，1 位小数封顶 10，单产品=10，成本缺失=null；雷达第六维 = 特性成本分均值 `featureCostAvg`（原整体 BOM 成本力废弃）；评分概览卡改为按特性明细（每行：特性名 + 评分 + 成本分，成本分 ≥8 绿 / ≥6 橙 / 否则红）+ 底部特性均值/成本竞争力均值。算法验证：竞品A 全特性最低→全 10 分均值 10；我方显示 450 vs A 380 → 8.4。tsc -b 0 错

49. **竞品演示数据（2026-08-12）**：`demo_data.sql` + `seed_demo_data.py`（项目根目录）——用户想直接看雷达/长城图效果。数据：我方项目 M270（5 模块 BOM ¥740：面板450/驱动120/电源60/结构80/包装30）+ 竞品三星 S27A800（¥643）+ LG 27UP850（¥722），五维评分 15 条，模块-特性关联 6 条（面板→显示、驱动→性能+规格、电源→可靠性、结构+包装→外观）。**幂等**（存在性检查，重复执行不重复）；副本验证全过（金额核对、与用户现有数据共存、幂等）。执行：关闭 costhub.exe → `python seed_demo_data.py`（自动备份自动找 db）；⚠️ 当时无法直接写入——用户 exe 正在运行（WAL 活跃），跨平台直接写有锁风险。看效果路径：对比分析 → 选 M270 + 勾选两竞品 → 生成雷达（雷达/长城图两视图都有料）

50. **低特效模式（2026-08-12，另一台电脑问题）**：用户反馈另一台电脑"设置打不开、整个界面发灰卡住"——初步判断：设置是 Modal 打开（App.tsx），antd Modal 遮罩动画在另一台电脑 WebView2 上卡住 → mask 常驻发灰、点击被拦截（与 2026-06-23 "Modal 不可见"同类动画异常）；叠加 15+ 处 backdrop-filter 毛玻璃（blur 最高 40px）在低配/远程桌面 GPU 上加重合成卡顿。排查发现 **data-motion 动效开关体系已在主题重构中移除**（CLAUDE.md 未同步），6/23 的应急开关没了。实现：①设置→个性化新增"低特效模式"开关（localStorage app-lowfx，ThemeContext 挂 data-lowfx 属性）——CSS 全局禁 animation + backdrop-filter，**不动 transition**（6/23 教训：transition 缩短会导致 Modal 动画异常）②prefers-reduced-motion 降级同步禁毛玻璃。tsc -b 0 错。**待用户确认根因**：另一台电脑是否远程桌面/虚拟机？WebView2 版本？（若老版本升级 WebView2 更治本；若远程桌面，低特效模式+换 Paper 主题即可）

51. **SKU 变体（2026-08-12，新功能）**：用户场景——一个产品多个 SKU（存储/规格微调），每 SKU 单独建项目太复杂；确认模型：**SKU = 基座 BOM + 差异规则（add/remove/replace）**，SKU 不落 BOM 只存差异，成本=基座+Σ差异（基座变自动联动，费率共用）。已实现：①新表 project_skus/sku_diffs（ensureSchema）+ CRUD ②项目详情「SKU 变体」tab ③SKU 详情弹窗（合并 BOM 按模块分组：绿新增/红移除删除线/黄替换 旧价→新价，仅展示不落库）④品类→项目→SKU 树状导航。⚠️ 教训：TS 里 `??` 与 `||` 混用必须加括号（TS5076，4 处）。**注意**：SKU 差异的 remove/replace 匹配基座按 名称+型号+模块，基座删除器件后差异会失配（界面有黄色警告提示）；SKU 成本未纳入成本快照/成本变动日志（后续可加）

52. **SKU 交互优化：Excel 式对比表（2026-08-12，用户反馈"设计不直观学习成本高"）**：原交互（先建 SKU 再进详情加差异，两步分离）被否。改为**对比表**：行=器件（基座+所有新增器件并集，按模块分组含模块小计行），列=基座（只读）+各 SKU（列头含整机成本），**改哪个格就是哪个 SKU 的差异**——黄底=与基座不同、删除线=已移除、改回基座值自动还原（saveCell 自动判断 replace/还原/remove/restore，无需理解"差异规则"概念）；单元格点击编辑弹窗（数量/单价/保存/移除该器件/恢复）；「＋新增差异器件」弹窗（目标 SKU 下拉+模块/名称/型号/数量/单价）；对比图移到表格上方；SKU 列表保留在下方（成本/较基座±/编辑删除）；详情弹窗降级为"查看全量 BOM"入口。实现：skuCompareRows useMemo（基座行+add 并集，按模块排序）+ saveCell 统一保存逻辑。tsc -b 0 错。**体验路径**：添加 SKU → 表格多一列（全等于基座）→ 直接改格子/加行，零概念负担

53. **SKU 差异 Excel 导入（2026-08-12，用户需求）**：批量加差异器件——与项目 BOM 导入同逻辑（Excel 列：模块/器件名称/型号/数量/单价）。**导入动作自动判定**（用户问"替换部分器件怎么做"——答案：Excel 里直接写新单价，基座有同名同型号自动识别为替换）：基座匹配且值不同→替换(橙)、值与基座一致且无差异→跳过(灰)、已有差异写回基座值→还原(蓝)、基座无→新增(绿)/更新已有新增；确认导入时**先清该 SKU 该器件的旧差异再写入**（幂等防重复行）；预览弹窗顶部选目标 SKU、异常行红色禁用确认（复用 _issues 检测：缺名称/单价非数字/单价0）。验证：动作判定 6 用例全过（含"同名模块不同器件→新增"场景）；tsc -b 0 错。**Excel 格式提示**：模块列同名没关系（同模块加新器件=新增，同名同型号=替换）

54. **SKU 行内编辑优化（2026-08-12，用户再反馈"还是复杂，Excel 直接删减替换就行"）**：弹窗式单元格编辑被否（点格子弹窗=两步）。改为**纯 Excel 手感行内编辑**：①点单元格直接变内联输入（数量+单价+✓/✕，回车保存）②**数量改 0 = 移除**（基座行）或删除该新增行 ③改回基座值自动还原 ④模块分组末行"添加器件到此模块"→ 填名称/型号后点某 SKU 列的 **＋** → 该列内联输数量/单价回车保存（applyCell 统一决策：qty<=0→remove/删add、等于基座→还原、否则→replace）⑤"已移除"格子点击恢复。旧 cellModal 弹窗删除。决策逻辑 8 用例全过；tsc -b 0 错。**当前交互全貌**：改数=点格子直接输、删减=数量清0、加件=分组内填行点列＋、批量=Excel 导入、SKU 增删改=下方列表

55. **BOM 导入"确认没反应"修复（2026-08-12）**：导入预览正常但点确认无响应——根因：①doImport 无 try/catch，任何 db 异常被 React 静默吞掉（界面无任何反馈）②每行器件匹配都串行 `getParts()` 查库，行数多时（完整 BOM 上百行）总耗时数十秒，看起来像卡死。修复：①doImport 整体 try/catch + message.error 显示真实错误（console.error 留档）②**器件匹配加缓存**（partCache Map：key=名称|型号，同名只查一次库）③**实时进度提示**（message key=「正在导入 X/N 条…」每 10 条刷新，用户明确知道还在导入中；Modal 确认按钮 onOk 返回 Promise 自动转圈）④导入开始 loading、结束销毁。tsc -b 0 错。⚠️ 教训：**用户可触发的 async 事件处理（onOk/onClick）必须有 try/catch + 用户可见的错误提示**，否则"点按钮没反应"极难排查；慢操作必须有进度反馈

56. **器件报价比对（2026-08-12，新功能，设计文档 `AI Agent 架构设计.md`）**：用户（成本经理）判断"谈价提醒鸡肋"（趋势是常识），真正价值=同物料跨项目报价差异；难点=命名不一致需 AI 疑似识别+人工确认。已实现：①表 part_aliases（别名沉淀，source=user_confirmed/marked_different，#NEG# 前缀存否定组合）+ part_compare_cache（品类+模块唯一，指纹失效）+ **part_insights（差异情报，unread 未读标记）** ②normalizePartName 归一化（全角转半角/小写/去空格横线）③BOM 模块头「跨项目比对」按钮 → 弹窗三区：已归组（规则实时+别名）/AI 疑似组（后台自动，机器人呼吸闪烁 ai-breathe，低特效降级）/状态条（上次识别时间+重新识别）④三层流程 AI 只建议不认定，确认沉淀、标记不同否定 ⑤指纹不变不重跑 AI，变化（改价/新项目）才识别；缓存显示过滤已确认行 ⑥**后台自动识别 + 报价情报中心**（用户要求：导入后自动后台识别、发现问题主动提醒）——doImport/BOM 增删改后 scheduleAutoCompare → 后台串行识别同品类全部模块（顶部进度条 X/Y，可慢慢跑）→ buildInsights（AI 疑似组必报 + 规则组价差≥¥10 且 ≥10% 也报）→ part_insights → 工具栏「报价情报」按钮+Badge 未读数+弹窗（确认同一/标记不同/已读）、模块头未读角标。**⑦修复（用户反馈"模型输出无法解析为 JSON"+"没看到自动识别提示"）**：AI 输出格式 JSON → **纯文本行（组名|序号1,序号2），parseAiGroups 双格式解析（先 JSON 兼容、失败降级文本行），去掉 format:json（小模型输出文本更可靠），错误信息带原文前 120 字便于诊断**；后台队列修复：**从 db 重拉项目列表**（导入新建项目 state 未刷新导致空跑）、sameCat<2 不跑、单模块失败计数、**完成总结 message（成功提示发现 N 条情报 / 失败提示可手动重试）**、**未配置模型明确提示跳过**。**⑧五轮自查（2026-08-12）修复**：upsertInsight **内容相同保持原状态**（原实现每次重建 unread → 用户已读后被任意触发重置红点复活）；解析/边界/竞态/低特效/安全面复查通过（Projects.tsx 无任何外部 HTTP 调用，全程本地）。**⑨"模型输出无法解析"再修复（2026-08-13）**：真实场景=模型回显输入清单（包装材料模块各器件本就不同，模型没找到疑似组就回显）——**解析不出 = 未发现疑似组**（返回空不抛错，"没找到"也是有效结论），console.log 留原文便于调优；6 用例全过（回显/正确格式/空/无/单序号/中文分隔）。**⑩"没发现自动识别"根因=数据问题（2026-08-13）**：库里 8 个显示器项目模块名对不齐（MNT-* 用"核心部件/驱动板/电源"、M270 用"面板模块/驱动板模块"、A001 用"显示模块/背光模块"）→ 跨项目凑不齐同模块多行 → 识别跑了也无情报。修复：①**新增演示项目 M280（¥758）/ M300（¥842）**，模块名与 M270 完全对齐（面板模块/驱动板模块/电源模块/结构件/包装材料），器件命名故意不一致（"27寸2K面板"vs"27寸液晶屏"vs"27'' 2K Panel"；驱动板同名不同料号）→ AI 疑似识别有料可出（预期 3 条情报：面板价差 ¥70 / 驱动板 ¥15 / 结构件 ¥10）②**识别引擎抽为全局 `src/autoCompare.ts`**（runAutoCompare：模型配置检查+Ollama fetch 探测静默跳过+全品类分组+串行识别+进度回调+情报落库），App 级驱动——**空闲自动**（10 秒无操作触发，5 分钟节流）+ **变更事件**（costhub-compare-request，导入/改价立即扫）+ 打开项目页触发；全局顶部进度条（机器人呼吸动画）+ 完成总结 message + costhub-compare-done 事件刷新红点 ③Projects 只留情报展示/手动比对弹窗（删本地队列，事件驱动）。验证：tsc -b 0 错。**⑫侧边栏入口 + 情报操作修复 + 审计日志本地对话（2026-08-13，用户三连需求）**：①「报价情报」入口移到**侧边栏**（nav 下方、用户区上方，简单一行 + BulbOutlined + 未读红点 Badge，全局任何页面可见；点击 → 跳项目管理页 + 打开情报弹窗（costhub-open-insights 事件））；项目页工具栏按钮移除；App 维护未读数（costhub-insights-changed 事件同步）②**情报确认/标记不同后"没反应"修复**：根因=只改 status 未重算内容，已确认的组还留在情报里——新增 rebuildModuleInsight（处理后重算该模块情报：确认的组被规则层吸收不再算疑似）+ 保持已读（红点不闪）+ 事件同步侧边栏 ③**审计日志收录本地 AI 对话**：LocalAIAssistant 发送消息成功/失败都写 ai_request_logs（request_type='local_ai_chat'，provider='Ollama（本地）'，system_prompt/user_prompt/response_summary 截断 2000 字），设置页审计日志 typeMap 加「本地AI对话」标签——外部 LLM 与本地对话统一审计。验证：tsc -b 0 错。**方向（用户提出，未实现）**：真正的 AI 应自行分析数据找出机会点（模块对比只是其中一种）——记入设计文档作为演进方向：AI 主动分析（价差/虚高/趋势/规格级对比/供应商维度）生成机会点清单，不限于用户指定的模块对比

106. **模块库明细单价全为 0 修复（2026-08-12）**：某项目项目管理有成本、模块库模块有成本，但点进模块看器件单价全是 0——根因字段错位：模块库明细表（itemCols）单价/小计/expTotal/bySubCat 用 dataIndex 'cost'（project_boms.cost 旧列，新导入行恒 0，仅老种子数据 79/179 有值），而数据源 getProjectBOMs 返回的是 part_cost（快照价，179/179 有值）→ 老项目碰巧正常、新导入项目全 0。修复：全部改用 part_cost 优先（`(i.part_cost ?? i.cost) || 0`），与项目页同一字段
107. **AI 请求日志增强（2026-08-11）**：审计页 AI 请求日志原来只有时间/类型/物料/提示词/响应——缺供应商、模型、Token 用量（数据有存但没展示，且日志记录时也没传）→ ①trendService 新增 lastLLMMeta 模块变量（调用 LLM 时记录 provider/model/token）②三处 saveAIRequestLog 补传元数据 ③审计页表格加「供应商/模型」「Token 用量」列 + 详情弹窗补全
105. **综合性排查（2026-08-11，真实数据库端到端校验）**：数据健康检查——无孤儿 BOM/竞品 BOM/洞察快照/供应商 ✓、无空快照列 ✓、快照机制生效 ✓、无已删残留 ✓；发现 parts.projects 字段与 BOM 实际引用不一致（122 处历史数据未更新，器件库"项目"列显示不全）→ 新增 syncPartsProjectsField（按 project_boms 重算去重，幂等）+ App 解锁后调用（实测将修复 74 个器件）
104. **第七、八轮排查（2026-08-11，修复 2 处）**：①删除当前选中的竞品后未重置 selectedCid → 停留在已删竞品的 BOM 视图 → 删除时若选中则清空选中+BOM ②Reports 报告页单价/小计还是 toFixed(2)（BOM 清单已统一 4 位）→ 改 4 位保持一致。核对安全：整机供应商加权报价原始值累加 ✓、竞品导入导出直接字段映射 ✓、Compare 页 estimated_cost→part_cost 口径一致 ✓、目标成本/评审/降本措施删除无级联依赖 ✓、Dashboard 竞品成本口径 ✓
102. **第五、六轮排查（2026-08-11，修复 3 处）**：①parts.projects 拼接——BOM 导入重复追加项目代号、定型时覆盖清空历史关联 → appendProjectCode 去重拼接工具（两处改用）②【严重】供应商价格历史从未工作——写入用不存在的 supplier_price_history 表（每次报错被 catch 吞），前端永远空 → 统一写入 part_supplier_price_history（ensureSchema 补 part_id/supplier_name 列）+ 读取同步改 ③核对安全：分解树递归删除子节点 ✓、加权成本 totalShare=0 边界 ✓、洞察快照 id DESC 排序 ✓、本地 AI 会话表结构 ✓
100. **第四轮排查：隐性 bug（2026-08-11，修复 4 处）**：①copyProject 复制 BOM 未过滤 is_deleted（软删的 BOM 也被复制）→ 补过滤 ②getProject/Dashboard 统计/快照项目查询未过滤 is_deleted（已删项目仍计入总数/平均成本/可被编辑）→ 全部补 COALESCE(is_deleted,0)=0 ③编辑 BOM 器件成本时只更新 parts 不同步 BOM 快照列 → 快照优先显示读到旧值（"改了没反应"）→ updateBOMItem 加 extra 参数同步快照（名称/型号/单价/分类），反向同步 module_items 也补全字段 ④核对安全：虚拟器件编辑有 part_id 保护、竞品无软删设计正常、删除项目后 selectedPid 重置 ✓
98. **第三轮排查：数据完整性（2026-08-11，修复 3 处）**：①copyProject 复制 BOM 只拷 5 字段丢快照列（part_name/part_cost/分类等）→ 复制项目依赖 parts 实时价、与源项目可能不一致 → 复制完整快照列+标记+created_at ②deletePart 直接物理删 parts 留下孤儿 BOM 引用（JOIN 到不存在的器件）→ 先软删引用 BOM 行 + 清理供应商/价格历史/洞察关联 ③deleteCompetitor 未清理 competitor_boms → 级联删除。核对安全：deletePartSupplier 删后重算加权成本 ✓、数据锁 Proxy 拦截 ✓、isAuthQuery 白名单窄 ✓、导出映射正确 ✓、deleteProject 软删设计 ✓
97. **深度排查 AI 与数据同步（2026-08-11，修复 3 处）**：①saveTrendItem 字段口径不统一——关注物料入口传 material_name（函数只认 query_category → 写入空值、source_type 默认 decomposition 而非 quick）→ saveTrendItem 统一支持 material_name/source_type/last_queried_at，关注物料入口补 source_type:'quick'（实测：关注物料进快捷洞察列表 ✓、分解树按 query_category 查询 ✓）②Decomposition 未监听 app-page-active——器件库标记/取消关注后切到洞察页列表不刷新 → 补监听（loadWatchedParts/loadQuickItems/loadTree 重载）③核对安全链路：saveTrendSnapshot 字段一致、Rollup 解析失败回退规则、AI 请求走 Rust 代理+审计日志、getActiveProviders 过滤 is_active、WorkLog 总结保存正常
96. **全盘数据错位排查（2026-08-11，修复 4 处）**：①recordProjectCostSnapshot / getSnapshotBOMDetail 用 INNER JOIN parts → 虚拟器件（part_id=0）被漏算成本——改 LEFT JOIN ②getSnapshotBOMDetail 查询 pb.created_at 但 project_boms 无此列（快照对比功能一直报错）——ensureSchema 补 created_at 列（旧数据默认当前时间）+ addBOMItem/addVirtualBOMItem 写入带时间 ③「从模块库导入」器件匹配只按型号（find p.model===）→ 改名称+型号全等 ④核对 updateLibraryModuleItem/updateBOMItem 三处同步（BOM 快照/parts/module_items）正确、竞品导入无匹配逻辑安全
95. **BOM 模块顺序与模块库一致（2026-08-11）**：项目管理 BOM 清单模块排列原来按数据出现顺序，与模块库分类顺序不一致——新增 sortedModNames（分类顺序优先 + 分类内按名称，与模块库同一套规则），模块分类从 modules 表取、分类顺序从 settings 读，切换项目时重新加载
93. **模块库对比精简（2026-08-11）**：①删除多模块对比（跨模块对比无意义，移除工具栏选择器+对比弹窗）②删除横向柱状图（单维度对比用图无意义）——模块成本列改为排名徽标（①②③彩色圆点）+ 最低价绿色标注 ③对比项目筛选器从顶部移到「器件级对比」区块内（只在需要器件级对比时出现）④修复：删除弹窗时误删了分类排序弹窗，已恢复
91. **参照对比开放所有项目 + 切项目自动退出（2026-08-11）**：①参照项目下拉不再限在研项目（isInDev 判断移除）——已完成项目也能选参照对比（排除当前项目自身，已完成项目选项带 ✓ 标记）②selectProject 切换项目时自动清空参照对比（refProjPid/refProjBoms/modRefMap 重置），要对比再重新选参照项目
89. **BOM 对比精度与参考一致性修复（2026-08-11）**：①BOM 清单小计、参考单价/参考小计/差异 全部 toFixed(2)→toFixed(4)（原 2 位先舍入再做差异，误差大）②参考匹配逻辑修复：原来「子类+（名称或型号）」模糊匹配会跨模块错配 → 改为「同模块+名称完全相等+型号完全相等」③导入参考器件 addBOMItem 新增 snapshot 参数——原来从 parts 实时价取快照（可能与参照项目快照价不一致）→ 现在传参照项目的快照值，导入后与参照完全一致（实测 2.3456 vs parts 3.0 场景验证通过）
87. **分类标签配色全面升级（2026-08-11）**：①getCategoryColor 改为「24 色自由池（与预设 8 色无重复）+ 哈希起点线性探测 + localStorage 持久化分配表」——同屏分类全部唯一不撞色（实测 24 分类 0 重复）、跨渲染顺序/跨会话稳定 ②器件库/竞品/仪表盘/模块库/项目/报告 全部改用 getCategoryColor（原来直接查 CATEGORY_COLORS 表，非预设分类返回 undefined 变灰）③另一台电脑显示灰色/相近色 = 旧 exe 未重新打包
85. **exe 文件夹图标修复（2026-08-11）**：任务栏图标正确（运行时用 assets logo）但文件夹里 exe 图标是旧图标——icons 目录图标过期（256x256 还是金色旧版）+ icon.ico 只有单帧。已用 src/assets/costhub-logo.png 重新生成全部 icons PNG（15 个尺寸，0% 像素差异）+ icon.ico（手动构造 7 帧 16-256px 32-bit ICO，Windows 可读）。重新 build 后 exe 图标 = 用户 logo
83. **BOM 对比项目移除（2026-08-08）**：项目管理 BOM 清单去掉「对比项目」下拉及模块/器件对比区块——与「参照项目」功能重复（都是与已完成项目对比），统一用参照项目（对比列+导入参考+行内编辑）
79. **器件库大类标签灰色修复（2026-08-08）**：全局 .ant-tag 的 border:none !important 干掉了 antd 6 彩色标签的边框（hex 色 Tag 靠边框呈现颜色）→ 器件库大类标签全灰；移除后恢复彩色
80. **登录框光标竖线修复（2026-08-08）**：size=large 输入框内文字行高与 40px 格子不匹配导致光标短——显式 line-height:40px + height:40px + caret-color 品牌色，光标撑满格子
81. **新环境首次登录密码错误修复（2026-08-08，严重）**：costhub.exe 单独放到新位置（无数据库）首次进入，666666 报错、设置密码也报当前密码错误——根因 getRawDb() 只 Database.load 不执行 ensureSchema，全新库 settings 表不存在，ensureAuthPassword 写入静默失败（catch 吞错）。修复：getRawDb 补 ensureSchema（幂等，schemaReady 保护），默认密码/明文/用户名正常初始化
75. **虚拟器件（成本占位）（2026-08-08）**：BOM 添加弹窗新增「虚拟器件（成本占位）」方式——不建 parts、不污染器件库，直接写 BOM 行（is_module_item=1 标记 + 名称橙"虚"标签）；getProjectBOMs 改 LEFT JOIN parts 支持 part_id=0；用于在研项目测算预估模块成本，定型/选型后可替换为真实器件（db.addVirtualBOMItem）
76. **页面激活刷新（2026-08-08）**：App.navigate 发 app-page-active 事件，模块库监听后重新 loadAll——修复"项目管理新增品类/定型后，切到模块库品类筛选无对应选项"（页面常驻不卸载导致的数据不同步）
77. **项目品类可自由输入（2026-08-08）**：项目编辑弹窗"品类"字段 Select → AutoComplete（可下拉选已有、可自由输入新品类）；保存时新品类自动入库 product_categories（saveProductCategory），品类筛选下拉同步出现
73. **在研项目参照测算（2026-08-08）**：BOM 工具栏加「参照项目（已完成）」下拉——选中后按模块名自动关联各模块参考（替代逐模块手选，替代 modRefMap 全量映射 + updateBOMRefProject 标记）；模块头加「导入参考」按钮（importRefModule：从参照项目复制该模块器件到当前项目，按名称+型号跳过已存在的，做加减法）；对比列（参考单价/参考小计/差异）+ 行内编辑按钮保留（就地改当前项目器件成本）
70. **模块分类排序（2026-08-08）**：模块库工具栏加「分类排序」按钮 + 弹窗（上下移调整分类顺序，getModuleCategoryOrder/saveModuleCategoryOrder 存 settings 表 JSON 数组）；loadAll 按分类顺序排序（未配置的分类排末尾按字母序）；恢复默认一键还原
71. **PPT 演示样式 HTML 文稿（2026-08-08）**：CostHub演示文稿.html（12 页，Apple 风格动画）——封面/平台概览/AI 架构图（中间 CostHub+左侧外部大模型可洞察+右侧本地模型可分析，数据安全边界标注）/9 套 Skill 体系（方法论+质量保障）/器件库/模块库/项目管理/对比分析/工作手账/数据安全/体验主题/结尾；每页一图一讲解，功能示意图（mini-table）呈现；方向键/空格翻页、F 全屏、Esc 退出、圆点跳转、触摸滑动、进度条、prefers-reduced-motion 降级
68. **主按钮文字看不清修复（2026-08-08）**：全局 .ant-btn-primary 的 background-image 高光渐变（!important）覆盖了 antd 6 的 CSS 变量背景 → 紫色主题下按钮背景变浅、白字看不见（hover 才恢复）——改为 ::before 伪元素实现内高光，antd 原生背景完整保留；登录按钮 .login-btn 类继续显式渐变+白字。影响范围：AI 起草/洞察行情/查看分解树/写便签等所有 primary 按钮
66. **登录页精致现代重设计（2026-08-08）**：深蓝硬朗风 → 暖白画布+Indigo/琥珀柔光晕染+氛围光斑；登录卡片白底毛玻璃+柔和双层阴影+顶部品牌色渐变细条+入场动画（420ms ease-out）；输入框聚焦 Indigo 光环；主按钮 Indigo 渐变+悬浮抬升+按压回压；Logo 容器 Indigo 光晕；提示条改用品牌色系。全部逻辑与隐藏功能（双击 Logo 显示密码）不变
64. **frontend-design 整体设计升级（2026-08-08）**：①排印体系：标题 26px/-0.03em 负字距、统计卡标签退后（11px/0.08em 大写）、数值 34px/-0.035em 等宽 ②表格节奏：表头更轻（10.5px/0.07em）、行高紧凑（9px）、分隔线更细 ③签名元素：统计卡数值用品牌色渐变文字（background-clip:text），让"钱"成为视觉主角，9 主题全部适配（glass 用青蓝渐变+发光）④按钮圆角 8px、标签胶囊、表单标签 550 字重 ⑤空状态/弹窗标题/分割线统一克制
62. **模块对比频闪修复 + 对比表列优化 + emil 打磨（2026-08-08）**：①频闪根因：对比项目多选 Select 渲染在 IIFE 内，筛选变更致整子树重挂载、下拉被强制关闭重开——cmpProjects/cmpProjOptions 提到顶层 useMemo（引用稳定）②器件级对比表每项目 3 列合并为 1 列（单元格内"单价×数量"小字 + 小计大字 + 最低角标），列宽大幅缩短 ③emil-design-eng：全部 transition:all → 精确属性；主按钮渐变+内高光；卡片柔和投影；导航激活态左侧品牌色条；金额 tabular-nums；危险按钮红系区分
60. **弹窗闪烁修复（2026-08-08）**：Apple 打磨中给 .ant-modal-content/.ant-dropdown 叠加的自定义 animation（modalMaterialize/popMaterialize）覆盖了 antd 内置动画名，导致设置弹窗等弹出层"闪一下"——已移除自定义动画，回归 antd 原生动画系统（ant-zoom/ant-fade，进出场完整）
58. **Apple 设计原则 UI 打磨（2026-08-08，纯界面）**：①反馈落在按压瞬间（按钮/卡片 :active 缩放 100ms）②过渡曲线统一 Apple 感 cubic-bezier(0.32,0.72,0,1) ③卡片悬浮抬升+按压回压、表格行柔和高亮 ④弹窗/下拉"材质浮现"（缩放+淡入同帧，materialize 而非纯 fade）⑤排印微调（标题负字距 650 字重、数值 tabular-nums、表头 0.06em、标签胶囊圆角）⑥细巧滚动条 ⑦prefers-reduced-motion 全量降级。不碰业务逻辑
56. **模块库器件级对比表（2026-08-08）**：模块库单模块对比视图（选 2+ 项目）新增「📋 器件级对比」表——行=器件并集（名称+型号匹配），每项目 3 列（单价4位/数量/小计），缺失项目显示 —，绿色标注该项目该器件小计最低；跟随「对比项目」筛选联动
54. **新增 elegant/glass 主题 + 互动体验优化（2026-08-08）**：①themes.ts 新增「精致现代」（暖白画布+Indigo 主色+柔和双层阴影+毛玻璃侧边栏+导航悬浮位移+卡片抬升）和「玻璃拟态」（深色渐变宇宙底+全组件毛玻璃 backdrop-filter+发光按钮/数值+玻璃弹窗/下拉/输入框+卡片顶部高光）两套主题，侧边栏用户菜单→主题 可直接切换（现有 7 主题保留，共 9 个）②index.css 新增约 100 行主题专属 CSS，覆盖 sidebar/nav/card/table/button/input/modal/dropdown/tabs/tag/scrollbar/内联浅色块/设置页左导航/登录页 ③纯 UI 改动，不动任何业务逻辑
52. **BOM 器件级横向对比（2026-08-08）**：项目管理 BOM 清单选「对比项目」后，模块成本柱状图下方新增「BOM 器件明细对比」——按模块分组，同名+同型号器件并排显示两侧单价(4位)/数量/小计 + 小计差（红色=对方贵，绿色=对方便宜，黄色=仅一侧有）；自动补出「仅当前有/仅对比有」的器件
49. **模块库对比增强（2026-08-08）**：①单模块对比视图新增「对比项目」多选筛选（图表+表格同步只显示选中的项目，ODM 比价场景）②工具栏新增「选择模块对比」多选 + 对比按钮 → 跨模块×跨项目分组柱状图 + 明细表格（行=模块 列=项目）
50. **模块库单一数据源改造（2026-08-08）**：模块库不再读 modules/module_items 而是**直接读 project_boms**（getLibraryModules/getLibraryModuleItems）——模块存在/器件/成本全部以 project_boms 为准（与项目页同一份数据）；写操作也直接改 project_boms（updateLibraryModuleItem 改 BOM 行+同步 parts、deleteLibraryModule 软删 BOM 行、renameLibraryModule 同步改名、复制模块=复制 BOM 行）→ **任何一边改动另一边自动反映，无需同步**。modules 表仅存分类/描述（getLibraryModules 读取分类，同名模块分类一致是有意设计 v2.3.5）；syncProjectModulesToLibrary 降级为兼容层
47. **模块库数据源补齐（2026-08-08）**：模块库只有 6 个模块 vs 项目管理 8 个项目完整模块——根因：①历史种子数据直接写 project_boms 没建 modules/module_items ②定型只同步 parts 不同步模块库。修复：新增 syncProjectModulesToLibrary()（从 project_boms 权威数据幂等补齐缺失模块/器件，实测 6→68 模块、12→189 器件）；App 解锁后自动调用；定型操作补调用
45. **模块分类标签自动配色（2026-08-08）**：模块库分类标签（卡片/对比表/标题）由固定紫色改为 getCategoryColor 自动配色——升级为 FNV-1a+雪崩混合哈希（稳定：同名分类永远同色，与渲染顺序无关；分布均匀）
42. **跨项目可视化对比（2026-08-08）**：①模块库模块对比视图新增横向条形图（各项目该模块成本对比，绿色=最低，点击表格行仍可展开明细）②项目管理 BOM 清单工具栏新增「对比项目」下拉 + 模块成本分组柱状图（当前 vs 对比项目逐模块对比，双色）
43. **DataTable 列设置按钮优化（2026-08-08）**：①按钮从绝对定位悬浮改为流式布局（表格上方右对齐，不再与工具栏按钮重叠）②BOM 模块明细循环内表格加 hideToolbar 去掉重复按钮，模块循环外 BOM 工具栏放统一「列设置」按钮（ColumnSettingsButton 组件，与 DataTable 共用 localStorage + window 事件联动，同 tableId 共享一份设置）
39. **DataTable 组件：Excel 式表格优化（2026-08-08）**：新增 src/components/DataTable.tsx——①列宽拖拽调节（表头右侧把手，localStorage 持久化）②列显隐设置（右上角「列设置」按钮 + 弹层勾选，持久化，可恢复默认）③长文本自动省略（内容列自动 ellipsis + 原生 title 悬停显示全文，行高固定单行）。已接入 25 张表（项目/器件/模块/供应商/竞品/报告/仪表盘/对比/降本），用法 `<DataTable tableId="唯一id" ... />`（其余 props 与 antd Table 一致）；操作列/动态列名表格不接入（如 Compare 动态对比表、目标成本表）

### 未完成 / 待办
- [ ] **整机供应商详情列表操作列 bug**：`record.part_id` 引用了器件供应商字段，整机供应商没有该字段 → 历史按钮失效（需改成查整机报价历史 `project_supplier_price_history`）
- [ ] 快捷洞察详情：融合总结目前用简单多数规则（上涨/下降计数），后续可升级为 LLM 生成更智能的融合
- [ ] 演示稿（CostHub演示稿.html）可替换真实截图（images/ 文件夹）

### 近期已知问题
- 旧数据库缺列问题：`trend_items.source_type`、`last_queried_at` 已加 ensureSchema 补列兜底；遇到其他 "no such column" 报错照此处理
- `renderSkillDimensions` 必须定义在组件主 return 之前（const 不提升，否则白屏）
- 洞察 Skill 多选用自绘勾选控件（Checkbox.Group 无 value 会全选）
- 快照排序：`getTrendSnapshots` 按 query_time DESC, id DESC，界面取 `[0]` 最新

## 关键约定
- **文档铁律（最高优先级）**：**每个任务完成后，必须立即同步更新两份文档——`CLAUDE.md`（功能模块/数据模型/更新历史）和本文件 `会话接续说明.md`（进度/待办/已知问题）**。顺序：改代码 → 改文档 → 才算完成。禁止攒到会话结束。
- 数据库 Schema 变更：`src/db.ts` ensureSchema 加 `ALTER TABLE ... ADD COLUMN`（幂等），旧库自动补齐
- 外部 LLM 无数据库访问通道（Rust 代理纯转发）；本地 Ollama 可读本地数据；成本占比等字段可发给外部（用户已确认）
- 版本号：文档内部一致（v2.3.18），与 package.json（2.3.14）不同步
- 构建：`build.bat` 或 `npm run tauri:build`；图标必须 8-bit（PIL 生成，ImageMagick 会 16-bit 报错）

## 技术架构速览
- 前端：React 19 + AntD 6 + Vite，页面在 `src/pages/`
- 后端：Tauri 2 + Rust，`src-tauri/src/lib.rs`（http_stream 等命令）
- 数据库：SQLite（costhub.db，exe 同目录），`src/db.ts` 封装全部操作
- 认证：SHA-256 哈希存 settings 表，db.ts Proxy 拦截实现受限模式

## 2026-08-14 公司电脑 Ollama 504 根治（已完成，最终根因）
- **现象**：公司电脑 PowerShell 连 Ollama（127.0.0.1）正常，应用内测试连接报 HTTP 504 + Squid HTML 错误页（<!doctype html><html xmlns=...>），反复多轮修复
- **最终根因（实锤）**：**reqwest 默认 features 含 system-proxy**——Windows 上通过 **WinHTTP** 读系统代理（`netsh winhttp show proxy`，公司**组策略**设置的，Windows 设置界面=WinINET 看不到也关不掉）。看似"无代理"的 `reqwest::Client::builder().build()` 仍自动套用 WinHTTP 代理 → `127.0.0.1:11434` 被转发给公司 Squid → Squid 连"它自己机器"的 localhost 失败 → **504 HTML 错误页**
- **修复（src-tauri/src/lib.rs）**：本地/私有网段（localhost/127.0.0.1/[::1]/10.x/192.168.x/172.16-31.x，is_local_url 解析 host 判定）直连分支必须**显式 `.no_proxy()`**（`reqwest::Client::builder().no_proxy().build()`）——单靠"不配 proxy"不够，system-proxy 特性会自动读系统代理
- **配套诊断**：504/502/408 时打印 headers（Server/Via/X-Cache）+ body 片段到 costhub-http.log（exe 同目录）+ 塞进返回 body 开头（报错弹窗直接可见 [costhub-diag] 行，含 direct_connect=yes(no-proxy) 标记）；前端测试连接显示 body 前 400 字符
- **前端**：LocalAIAssistant/autoCompare 候选地址 localhost 配置时 127.0.0.1 优先
- **⚠️ 铁律**：任何"本地直连" reqwest client 必须 `.no_proxy()`；排查代理问题先跑 `netsh winhttp show proxy`（WinHTTP 与设置界面 WinINET 独立）；GUI 启动 eprintln 不可见，诊断写文件或塞回 body
- **验证**：公司电脑拷新 exe 测试连接成功 ✅；cargo check/tsc -b 干净；全部推送 costhub
## 2026-08-14 自主分析引擎（autoAdvisor）+ 报价情报体验修复（已完成）
- **报价情报修复**：confirmInsightGroup/rejectInsightGroup 缺 loadInsights()（库已重算但弹窗不刷新）→ 补齐；弹窗加「待处理/全部」Segmented（待处理=还有情报组）；确认后提示潜在节省（最高-最低单价×数量）；标记不同加 try/catch
- **自主分析引擎 src/autoAdvisor.ts**：App 级 60 秒轮询（scheduleAppAdvisor，与 runAutoCompare 同机制，独立防重入）
  - 规则层（纯函数 buildRuleCandidates，vitest 7 用例）：①项目成本 ≥60 天未变动（快照留痕）②大额物料（≥¥10）≥90 天未调价（parts.updated_at）③领域超目标 ≥5%（computeTargetStatuses）④大额物料单一供应商
  - AI 润色：Ollama /api/chat 非流式（http_post），资深成本经理视角优化标题/建议/提示词；30 分钟节流（settings advisor_ai_last_run）；失败静默降级规则文案；logLocalAICall(request_type='auto_advisor') 全程留痕
  - 指纹去重（spc|pid / spp|partId / tg|pid|domain / ss|partId），dismissed 后可重新提醒
- **数据**：ai_advisor_insights 表（core.ts coreCreate 新增）+ db/advisor.ts CRUD
- **UI**：LocalAIAssistant 右栏顶部「自主建议」折叠区——Badge 未处理数、立即分析按钮、上次运行时间、卡片（类型图标/标题/详情/提示词块）、操作：📋复制提示词 / 🔍生成行业洞察（agentSearchLoop('price-trend') 回填 detail）/ ✓已处理 / 忽略；「仅看待处理/显示全部」切换；App 发现新建议 message 提醒
- **验证**：73 vitest 全过、tsc -b 0 错、exe 构建成功、已推送 costhub
## 2026-08-14 报价识别分批处理（用户反馈"转半天只出1个模块"）
- **问题**：runAutoCompare 一次串行识别全部品类全部模块，每个无缓存模块调一次 Ollama（20-60s/模块），几十个模块=转半天；单模块卡住拖死整轮；结果不透明
- **修复（autoCompare.ts + App.tsx）**：
  1. **分批**：每轮最多识别 3 个需 AI 的模块（BATCH_SIZE=3，缓存命中秒过不计批容量），跑完即停，App 60 秒轮询自动续下一批 → 结果渐进出现
  2. **单模块 60s 超时**：超时直接跳过不重试（其他偶发错误仍重试一次）；超时后取消流监听
  3. **进度透明**：进度条显示"剩余 N 个模块分批自动继续"；批小结 message（有情报/失败才弹，无异常静默）；全部完成时总结
- 验证：73 vitest 全过、tsc 0 错、已推送 costhub
## 2026-08-15 本地-云端桥 aiBridge（三步链路 + 发送前审计）
- 见 CLAUDE.md「本地-云端桥 aiBridge」节；9 测试用例全过；待加入演示文稿讲解

## 2026-08-15 全面审查 15 轮交付汇总（目标：成本管理完美工具）
**已交付功能**（前序对话累计）：
- 反馈条带撤销（确认/归档/建议处理 notification+撤销）、批量操作（报价情报多选批量确认/归档+撤销）
- 全局 emoji→AntD 图标（iconMap 103 映射 + EmojiIcon 组件，UI 页全部接入）
- 物料洞察树升级：洞察状态点/成本热度/大头优先/血缘链高亮/面包屑/双击聚焦（hover 120ms 防抖、聚焦可点击取消）
- 极简白主题（Apple 风浅色实底：无边框白卡/胶囊控件/系统蓝，第 10 主题）
- 云端用量管控：每日阈值（默认 50）+ Dashboard 用量格 + 设置弹窗
- 双向 AI 洞察：本地-云端桥（脱敏模板/严格审计/拦截留痕/7天复用/多轮工具调用）
- 成本变动追溯链：改价写 part_supplier_price + 加权成本写 part_cost 日志（含 impact_scope）+ 器件库弹窗展示
- 空库首启引导、AIGuide 8 项能力更新、审计日志拦截/失败红色标识

**审查修复的 bug（本轮 15 轮）**：
1. 报价情报"确认后组重现"深层根因：buildInsights 规则组缺全行已确认过滤（后台轮询路径）
2. getProjectModuleSummary 缺软删过滤 → 模块成本虚高
3. 快照无实质变化也插入 → 空快照泛滥（已去重）
4. 深色主题适配：状态仪表/分解树卡片硬编码白（12 处）、chartTheme 仅 macos 适配（dark/glass 图表文字看不清）
5. 供应商改价不写 cost_change_log（追溯链断裂）
6. 调试日志 29 条清理、demo-echarts 1.1MB 出版本控制
7. 其他：批量确认撤销引导、切视图清空选择、聚焦取消可点击、Dashboard 用量格可点击、云端工具参数短码拦截

**验证**：96 vitest 全过、tsc -b 0 错、构建成功、costhub 远程同步
**说明**：设计方案-Demo.html 保留为设计稿（含 Apple/极简白/深色驾驶舱/科技感主题演示）


## 第 17-18 轮（2026-08-15 深夜）
- **受限模式提示条**：进入受限模式后 App 顶部常驻琥珀提示条（数据锁定说明 + 「返回登录页解锁 →」），与登录页勾选说明双保险，防止误以为数据丢失；解锁走 handleUnlock 自动清数据锁
- **调试日志清理**：移除 38 条 console.log（Decomposition 14 / SupplierManagement 6 / WorkLog 8 / ollama 1 / trendService 每次搜索日志 9），保留设置页「测试连接」诊断日志与一次性迁移/建表日志
- **舍入铁律复查**：71 处 reduce 全部合规（舍入均在累加完成后/显示层），14 处疑似全部确认无违规
- 96 测试通过 / tsc 0 错 / exe 18.3MB / 推送 c0a1fab

## 第 19 轮（2026-08-16）：演示稿收尾 + 整机供应商历史按钮修复
- **演示文稿补 aiBridge 新页（清掉上轮待办"待加入演示文稿讲解"）**：`CostHub演示文稿.html` 由 18 页 → **19 页**——P13 物料洞察后新增 **P14「AI 双向洞察 · 本地-云端桥」**（三步链路卡 ①本地读建议卡→只输出 物料名/品类/问题 三字段 ②固定脱敏模板发云端查行情+发送前正则审计 ③本地结合本地数据+云端行情给结论；🔐 安全卡=云端 prompt 固定模板三个字段位、成本/供应商/代号结构上无处可传+审计拦截+全程留痕+每日用量阈值；♻️ 防重复卡=7 天内复用确认）；后页顺延 P15-P19（演示生成/边界/教程×2/规划结尾）；`CostHub演示文稿-演讲逻辑.md` 同步 19 页版（章节时间表 AI 主戏 P11-P16、P14 讲法三步、重点页五个、Q&A 新增"外发提示词会被看到吗"）；HTML 结构验证 19 section 开闭平衡、243 div 开闭平衡
- **修复：整机供应商历史按钮失效（已知 bug 清单清项）**：供应商管理页两个详细列表的操作列历史按钮全失效——`sup_odm_list`（ODM）行引用不存在的 `record.part_id`（ProjectSupplier 结构无该字段）；`sup_detail`（器件供应商）行对象映射漏 `partId` → 点击传 undefined。修复（src/pages/SupplierManagement.tsx）：detailListData 补 `partId: part.id`；新增 `showProjectPriceHistory(supplierId)`（查 `project_supplier_price_history`，弹窗列结构一致直接复用）；ODM 按钮改 `showProjectPriceHistory(record.supplierId)`；器件表按钮改 `showPriceHistory(record.partId, record.supplierName)`（供应商地图弹窗 sup_detail_parts 原已正确不动）
- **清理调试残留**：删除 `_dom-dump.html` / `_shot.html` / `_shot.png`（上一会话验证演示稿渲染留下的）
- **验证**：tsc -b 0 错；96 vitest 全过（10 文件 1.9s）；⚠️ 本环境 vitest 必须用默认 reporter 跑（`npx vitest run`），`--reporter=basic` 会报 "Failed to load custom Reporter from basic"（vitest 4 兼容问题，与代码无关）
- **已推送**：见提交记录（演示稿 19 页 + 供应商历史修复）

## 第 20 轮（2026-08-16）：AI 工作台——打开就有"本地 AI 在工作"的感觉
- **需求**：用户反馈"没有那种一打开这个工具，这个本地AI就在工作的感觉"——原来自主引擎（advisor/compare）后台静默跑，无状态呈现、无主动输出
- **AI 状态条**（src/components/AIStatusBar.tsx，驾驶舱顶部）：三态（连接中呼吸动画 / 已连接绿点+模型名 / 未连接灰点+点击去配置）；探测逻辑 src/aiStatus.ts detectOllama（127.0.0.1 优先复用 no_proxy 铁律，30 秒自检）；监听全局事件 `costhub-ai-task`（{task, done}）实时显示当前任务 + "刚刚完成 X · HH:mm"
- **AI 今日速览**（src/dailyBrief.ts + src/components/DailyBrief.tsx）：打开驾驶舱即触发——collectBriefFacts 本地规则收集事实（项目/器件/大额物料 ≥¥10/AI建议/报价情报/待办，任一查询失败不阻断）→ 本地 Ollama 润色 3-5 句自然语言（http_post /api/chat 非流式 temperature 0.3，30 分钟节流原则）→ 失败静默降级规则文案 buildRuleBrief；节流：settings ai_daily_brief 存 {date, hash(FNV-1a 指纹), text, model}，同日同指纹复用缓存，数据变化自动重生成，「重新生成」按钮 force；logLocalAICall('daily_brief') 留痕
- **AI 活动记录**（LocalAIAssistant 对话区顶部折叠条）：getAllAIRequestLogs 最近 15 条 → AI_TYPE_NAMES 中文映射 + 时间 + 摘要 + 成功/失败红蓝 Tag——"AI 今天都做了什么"可回溯
- **启动可见性**（App.tsx）：advisor/compare 接 onProgress 广播 costhub-ai-task（"报价识别：模块名"/"自主分析：读取项目与 BOM…"），完成广播 done
- **新增测试**：src/__tests__/dailyBrief.test.ts 9 用例（hashString 稳定性/不同输入不同输出、localDate 格式、buildRuleBrief 有/无提醒场景+大额物料、buildBriefPrompt 约束与事实携带）
- **验证**：tsc -b 0 错；105 vitest 全过；构建出 exe（AI 工作台版）
- ⚠️ 待用户试用反馈：速览文案风格、状态条是否干扰、活动记录粒度（后续可加打字机效果/更多事实维度）

## 第 21 轮（2026-08-16）：关键物料自动洞察 autoInsight（用户确认节流设计：30 天 + 变化触发 + 复用）
- **需求**：根据项目类型自动识别关键物料做行情洞察，但不要天天洞察浪费 token；用户确认默认 30 天轮询 + 变化立即触发 + 7 天复用
- **设计原则**：识别免费（纯本地规则）、触发吝啬（四道闸门）、呈现主动（驾驶舱卡片）
- **识别**（src/autoInsight.ts）：帕累托——每项目 BOM 按小计（单价×数量，原始值）降序，累计占比 ≥80% 或 top5（先到者，至少 2 个才截断）；数量 0/软删/总成本 0 跳过；跨项目按 materialKey（名称+品类归一化，复用 db/advisor）聚合去重——同一物料多项目关键只洞察一次
- **四道闸门**（buildInsightPlan 纯函数）：①首次→立即洞察 ②7 天内→reuse 复用（显示上次方向/置信度/摘要/行动）③30 天周期内（settings ai_insight_interval_days 默认 30）→wait（"N 天后到洞察周期"+下次日期）④超期→insight；预算闸门：与云端每日上限共用（ai_usage_cloud_daily_limit 默认 50），超限→wait"明日自动继续"
- **执行**（runAutoInsight，App 级 60 秒轮询+启动立即一轮，insightRunningRef 防重入，limit=3 分批渐进）：agentSearchLoop('price-trend')（云端搜索+分析，外发=物料名/品类，与快捷洞察同链路同安全边界）→ 落 trend_items(source_type='auto') + trend_snapshots(source_type='auto')（Decomposition 快捷洞察区可见）；完成广播 costhub-insight-done；有洞察才 message
- **UI**（src/components/KeyMaterialInsights.tsx，驾驶舱 DailyBrief 下方）：物料名/型号/品类 Tag/占某项目 X%/共 N 项目；已洞察→方向 Tag（↑红↓绿～蓝）+置信度+摘要+行动建议 Tooltip+洞察日期；wait→原因+下次日期；首识→"待自动洞察（下一轮轮询触发）"；「全部洞察 →」直达物料趋势洞察页；无项目/无关键物料不渲染；监听 costhub-insight-done 自动刷新
- **公共函数**：queryLastInsights()（引擎与 UI 共用，materialKey 索引 trend_items quick/auto 最新 snapshot）
- **新增测试**：src/__tests__/autoInsight.test.ts 13 用例（帕累托截断/top5 上限/占位跳过/软删跳过/跨项目聚合/品类不合并/首次/复用/等待+下次时间/超期/预算/自定义周期/时间解析）
- **验证**：tsc -b 0 错；118 vitest 全过；构建出 exe（自动洞察版）
- ⚠️ 待办：设置页可调洞察周期 UI（当前仅 settings ai_insight_interval_days 可读）；变化触发（价格变动≥5% 立即洞察）第一版未做（首次识别天然覆盖新物料，后续加变化检测）
- **平台化讨论待续**：用户想"打造成 DSH 式工具"——本轮已给评估：推荐 P1 工具注册表+复合任务对话、P2 MCP 桥（不直接嵌 DSH 内核，形

## 第 22 轮（2026-08-16）：洞察列表联动 + 已处理建议可见性修复
- **用户反馈 1**："洞察完的物料放到洞察列表里面去可以查看"——根因：autoInsight 落库 source_type='auto'，而 getQuickTrendItems 只查 'quick' → 自动洞察物料不进洞察列表。修复：查询条件改 IN ('quick','auto')；快捷洞察卡片标题旁 source_type='auto' 显示橙色「自动」Tag（与手动洞察区分）；点击卡片详情/历史/追问全复用
- **用户反馈 2**："AI 建议处理完之后在已处理里面是空的"——排查三层：①查 release 库：16 条建议全 open 无 done；UPDATE SQL 层验证正常（rowcount=1）②根因一（真实 bug）：autoAdvisor enhanceWithAI 的 AI 润色用 updateAdvisorStatus(existing.id, 'open', ...) 覆盖用户状态——findAdvisorByFingerprint 把 done 也算"已存在"，30 分钟后润色把 done 改回 open → 已处理记录消失。修复：润色前查 status，非 open 跳过 ③根因二（交互）：setAdvisorStatus 乐观移除（点击即消失）依赖 loadAdvisor 恢复，任一步失败列表就空 → 改乐观标记（条目保留立即显示新状态）+ 失败回滚；「仅看待处理」按钮加"（已处理 N）"计数；处理成功提示"切「显示全部」可查看"
- **验证**：tsc -b 0 错；118 vitest 全过；构建出 exe（修复版）态/价值/成本三方面都不划算）；等用户了解后再定

## 第 23 轮（2026-08-16）：自动洞察改为按子类（通用名称）
- **用户要求**：自动洞察不要按特定物料，按子类——子类一般是物料的通用名称（如"液晶面板"），行情洞察对通用名更有意义，且避免具体型号外发
- **识别改造**（src/autoInsight.ts）：identifyKeyMaterials 改为按 sub_category 分组（sub_category 为空回退 part_name）→ 组小计降序帕累托（累计占比 ≥80% 或 top5，至少 2 组截断）；KeyMaterial 结构去掉 partId/model/unitCost/quantity，加 models（该子类涉及的具体型号，去重最多 5 个，UI 显示"含 N 种型号"）
- **聚合**：MaterialAggregate model → models（跨项目合并去重）；materialKey 仍按（子类名+大类）归一化
- **洞察**：agentSearchLoop 传子类名（通用名称）+ main_category；落库 trend_items query_category=子类名（与手动快捷洞察 key 一致，7 天复用/30 天周期自动衔接）
- **UI**（KeyMaterialInsights）：显示子类名 + 品类 Tag + "含 N 种型号" + 占项目 X%；标题 Tag 改"按子类 · 30 天周期"；App message 改"N 类物料行情"
- **测试更新**（14 用例）：同子类多型号合并帕累托（面板A+面板B=71%）、top5 组上限、sub_category 空回退物料名、数量 0 跳过、软删跳过、跨项目型号去重合并、大类隔离、闸门 6 例、时间解析
- **验证**：tsc -b 0 错；119 vitest 全过；构建出 exe（按子类版）

## 第 24 轮（2026-08-16）：P1 Agent 工作台——工具注册表 + 复合任务对话（用户确认先做 P1）
- **目标**：把本地 AI 对话升级成"指挥 AI 干活"的工作台——一句话完成多步成本分析（DSH 式体验第一步）
- **工具注册表**（src/aiTools.ts，12 个只读工具）：query_projects / query_project_bom / query_project_cost（模块成本结构）/ query_part_suppliers / query_supplier_trend / query_target_status / query_cost_snapshots / query_price_insights / query_advisor_insights / query_worklog / query_todos / insight_material_trend；统一 AiTool 结构 + validateArgs 参数校验 + executeTool 截断 2000 字；⚠️ 第一版无任何写操作工具（安全边界）
- **Agent 循环**（src/aiAgent.ts）：计划-执行-总结——本地模型读工具清单输出 JSON 计划（buildPlanSystemPrompt：steps 数组/依赖顺序/最多 6 步/无需工具输出空 steps）→ parseAgentPlan 容错解析（代码块围栏/中文引号/单引号/尾逗号/别名映射）→ runAgentPlan 顺序执行（onStep 轨迹回调）→ 基于执行报告流式生成最终回答（buildAnswerSystemPrompt：禁止编造）
- **UI**（LocalAIAssistant）：输入框上方 Segmented「💬 普通对话 / 🤖 Agent 任务」；Agent 模式输入区上方执行轨迹卡（✅/❌/⏳ 工具中文名+参数+结果摘要，可折叠）；sendAgentTask：计划→执行→流式总结；无需工具/解析失败自动降级普通对话
- **审计**：logLocalAICall('agent_plan'/'agent_answer') 留痕
- **测试**：aiTools 7 用例 + aiAgent 12 用例（容错解析 7/未知工具/提示词）——139 vitest 全过
- **待用户试用**：Agent 模式的计划准确性（本地模型 qwythos-9b 输出 JSON 质量）、轨迹展示是否清晰；后续可加写操作工具（需用户确认）、更多工具（SKU/ODM/报告）

## 第 25 轮（2026-08-16）：Agent 子类对比 + 驾驶舱去杂乱 + 图表优化（用户试用反馈）
- **用户反馈 1**："Agent 任务读不到成本数据 / 没法按子类对比成本"——模型只看到项目代号，缺对比工具。新增 **compare_subcategory_cost**（第 13 个工具）：跨全部项目按 sub_category 精确匹配聚合（子类小计 + 占项目 BOM 比例 + 最高/最低项目与差额），直接回答"同一子类在不同项目的成本高低"
- **用户反馈 2**："驾驶舱信息越来越多非常杂乱"——AI 状态条 + 今日速览合并为一张卡（src/components/AIWorkspace.tsx，虚线分隔，AIStatusBar/DailyBrief 加 compact prop 去卡片样式），顶部三卡变两卡
- **用户反馈 3**："新装了个图标优化的 skill……针对工具里面不同图表的特征根本性优化"（用户后纠正：是**图表** charts 不是图标）——按 ui-ux-pro-max skill 的图表选型规则（Compare Categories：比较类柱状图必须降序 + same hue family 统一色系）：驾驶舱两个 BOM 成本柱状图（项目/竞品）按值降序排列，颜色从每柱彩虹色改统一主色系（最高值 #1D4ED8 深蓝高亮、其余 #60A5FA）
- **顺带**：工具图标语义化（TOOL_ICONS/toolIcon 13 个工具按数据特征配 AntD 图标，Agent 轨迹显示；⚠️ .ts 不能写 JSX → 存 React.ComponentType 引用 UI 层实例化）
- **测试**：aiTools.test.ts 10 用例（新增子类对比必填校验 + 全部工具图标覆盖）——**141 vitest 全过**
- ⚠️ 图表优化只做了驾驶舱两图；其他页面图表（ModuleLibrary/Compare/Decomposition 等）可按 skill 规则继续审计（排序/饼图≤5类/色系统一）——待用户确认范围

## 第 26 轮（2026-08-16）：关键物料机会点建议 + 驾驶舱清爽化（用户反馈"只给泛泛结论/还不够清爽"）
- **用户反馈 1**："本地大模型找出关键物料洞察完之后应该对具体物料有个建议，特别是有机会点的，而不是只泛泛给出洞察结论"
  - 新增 **buildMaterialSuggestion 机会点/风险点引擎**（autoInsight.ts 纯函数，vitest 6 用例）：
    - 行情下降+置信度高/中 → 🎯机会点：标题"「X」行情下行（幅度）" + 行动"占 M270 BOM 45%，建议降价谈判/重新询价，占比≥30% 追加'优先处理'"
    - 行情上涨 → ⚠️风险点：建议锁定价格/备货，占比高追加评估替代料；幅度≥10% 强调"涨幅明显"
    - 低置信度 → 追加"先观察 1-2 周 / 先跟踪行情"；无方向 → info（维持节奏，下次周期自动复查）
  - LastInsightInfo/InsightPlanItem 补 magnitudeMin/magnitudeMax 透传（queryLastInsights 从快照取）
  - UI（KeyMaterialInsights）：机会点绿色边框+🎯 Tag、风险点红色+⚠️ Tag，行动文本直接展示在洞察行下方（不再藏 Tooltip）
- **用户反馈 2**："驾驶舱感觉并没有看起来清爽"
  - 关键物料洞察移入「目标成本达成 | 关键物料洞察」双列 grid（compact prop 去 marginBottom），AI 洞察建议区 gridColumn '1 / -1' 全宽——顶部纵向大卡从 5 行缩到 4 行，左右宽度充分利用
- **验证**：tsc -b 0 错；147 vitest 全过；构建出 exe（机会点版）
- ⚠️ 若仍觉乱：下一步可把「AI 洞察建议/最近成本变动」收进 Collapse 折叠组（默认收起），或按区块做 Tab

## 第 27 轮（2026-08-16）：驾驶舱全模块缩略 + 机会点置顶（用户要求"展示模块都要能缩略只显示几条，机会点放上面"）
- **关键物料洞察**：默认只显示 4 条 + 「展开全部（N）▼/收起 ▲」（expanded state）；加载时按 机会点(0) > 风险点(1) > info(2) > 其他(3) 排序置顶（buildMaterialSuggestion 判级，同级别保持原顺序）
- **目标成本达成**：默认 3 条 + 展开（targetOpen state）
- **AI 自主建议**：默认 3 条 + 展开（advisorMore state）
- 最近成本变动（costOpen）/ 巡检发现（auditOpen）原有展开保留——**驾驶舱所有展示模块都支持缩略展开**
- **验证**：tsc -b 0 错；147 vitest 全过；构建出 exe（缩略版）

## 第 28 轮（2026-08-16）：AI 学习引擎——对话中学习"关注什么、什么逻辑是对的"
- **用户需求**："针对本地大模型的思考逻辑，应该在日常工作与他的对话中他能够学习，我更关注的是什么，什么样的逻辑是对的"
- **设计边界**（诚实说明）：本地模型不做微调，用"规则注入"实现行为修正——务实、可审查、全本地
- **① 关注主题统计**（src/aiLearning.ts）：6 大主题（项目成本/物料行情/供应商/目标达成/成本结构/工作安排）×关键词表；classifyUserQuestion 纯函数命中排序；trackUserFocus 每次发消息自动累计到 settings ai_user_focus（mergeFocus 旧数据衰减一半≈30 天窗口）
- **② 逻辑偏好规则**：对话气泡底部 👍/👎 反馈——👍轻提示；👎弹窗（太泛泛/没结合本地数据/结论逻辑不对/没优先关注点 + 自定义输入）→ addLearnedRule 沉淀"必须遵守的逻辑偏好"（settings ai_learned_rules 最多 20 条，可删）
- **③ 偏好注入**：buildPreferenceContext = 「用户近期关注重点（优先覆盖）」+「已学逻辑偏好（必须遵守）」→ 注入普通对话 system prompt + Agent 任务计划/总结 prompt
- **UI**：反馈行（"教 AI 理解你的偏好"）；左侧「🧠 AI 学习档案」按钮 → Modal（关注主题 TOP 条形图 + 规则列表删除）
- **测试**：aiLearning.test.ts 13 用例（分类/排序/聚合衰减/反馈转换/上下文）——**160 vitest 全过**
- ⚠️ 后续可加：关注主题影响今日速览/自主建议排序；纠正消息自动识别（隐式学习）；规则影响 autoAdvisor 润色 prompt