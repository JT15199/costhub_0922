## 当前进度（2026-08-14 会话：AI 能力嵌入工作流 + 工程优化）

### 工程优化（已完成，已推送 GitHub costhub）
- 统一版本号 2.3.19；删除废弃页面（TrendInsight/AnalysisChecklist/CostControl）；.gitignore 完善
- 性能：页面级 React.lazy（主包 4.2MB→302KB）+ ECharts 按需注册（src/echartsSetup.ts）+ 虚拟滚动
- 可维护性：**db.ts 按业务域拆分为 src/db/ 11 文件**（core/auth/parts/suppliers/projects/competitors/compare/trend/settings/worklog/dashboard），db.ts 仅聚合导出；生成工具 _tools/split-db.mjs
- 可靠性：vitest 测试框架（30 用例：SKU 成本/normalizePartName/目标达成/项目体检）+ 16 个高频查询索引

### AI 能力真实落地（用户核心诉求：AI 要真实有效用起来）
- **仪表盘 → 驾驶舱**（Dashboard.tsx + src/targetInsight.ts）：目标成本达成预警置顶（未达标项目/领域红色卡片，点击直达项目页）+ AI 今日洞察区（报价情报/成本快照异动）+ 跨页直达事件 costhub-open-project
- **项目页 AI 体检条**（Projects.tsx + src/projectHealth.ts）：规则自动检查 4 类问题（BOM 缺单价/数量0、目标超支、快照异动、同类报价偏高），点击直达对应 tab；「AI 小结」按钮用本地模型一句话概括（失败静默）
- **成本快照对比 AI 解释**：对比弹窗内「生成解释」——差异清单交本地模型流式生成原因说明
- **全局 AI 问询**（src/components/GlobalAI.tsx）：侧边栏入口，带当前项目上下文（localStorage costhub-ctx），回答可一键记入工作手账（带项目标签）；数据安全边界=只有本地模型读数据
- **首次 AI 能力引导**（src/components/AIGuide.tsx）：解锁后首次显示 5 项 AI 能力 + 配置就绪度，一键去设置

### 用户现状配置（生产库）
- 云端：DeepSeek 官方激活（deepseek-v4-flash）；搜索 provider 全部未配 key（趋势洞察依赖 DeepSeek 原生搜索兜底）
- 本地：Ollama qwythos-9b:latest（报价情报/体检小结/全局问询/快照解释都靠它）
- ⚠️ 项目目标成本数据极少（仅 2 条）——驾驶舱预警依赖目标设定，建议用户先在「项目管理 → 成本分析」设定领域目标

### 待做（规划 AI Agent 架构设计.md 未启动项）
- 规格级项目预估（拿最像旧项目改规格估差异）
- 界面轻量化（12 导航→4 区、项目工作台 1 屏化、项目卡片流）
- 谈价提醒（降级为能力储备）
- 器件供应商价格趋势 AI 小结（②嵌入点之一，未做）

# CostHub 项目接续说明

> 本文件用于新开对话时快速接续上下文。**铁律：每次任务完成后立即更新本文件 + CLAUDE.md**（先改代码 → 再改文档 → 才算完成），不攒到会话结束。

## 当前进度（2026-08-13 会话）

### 方向讨论（设计文档已落盘：`AI Agent 架构设计.md`）
- **用户反思**：功能多但交互重，日常操作不如 Excel 便利；工具定位 = 不做 Excel 能做的事，只做数据沉淀 + 自动分析；交互原则 = Excel 手感（点格即改/清0即删/自动算/不弹窗），录入靠 Excel 导入导出
- **器件报价比对**（设计定稿，待开发）：同物料跨项目报价差异情报——三层流程（规则分组→本地 AI 疑似识别→人工确认沉淀别名映射 part_aliases 表）；范围=同品类全部项目、按模块切片（模块头「跨项目比对」按钮，嵌项目 BOM 清单）；AI 只建议不认定；SKU 间不比（SKU 对比表已覆盖）
- **后续路线**：规格级项目预估（拿最像旧项目改规格估差异）、界面轻量化（12 导航→4 区、项目工作台 1 屏化、项目卡片流）、谈价提醒降级（用户判断趋势是常识，价值有限，链路留作能力储备）

## 2026-08-12 会话：演示文稿重构（非代码改动）