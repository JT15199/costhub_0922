# CostHub 物料成本洞察模块 · Skill方法论体系与差异化输出结构 v7

> 本文档定义完整的五个预置Skill方法论，并**修正v5/v6文档中的一个设计缺陷**：此前"洞察结果结构化重排"设计的五个维度（供需格局/价格驱动/卡点识别/政策地缘/展望）被当作所有Skill共用的固定输出模板，这是不对的——不同Skill应有各自对应方法论的输出结构，而非千篇一律。请与v3、v5、v6文档配合阅读，本文档中的输出结构设计**取代**v5文档第2.2节中固定五维度的表述。

---

## 一、核心设计原则

**每个Skill不仅是"搜索时关注的角度"不同，其最终呈现给用户的结果结构也必须体现该方法论的独特视角。** 如果五个Skill搜索完之后都套用同一套输出模板展示，方法论的差异化就失去了意义——用户选择"波特五力"和选择"库存周期"，应该看到两种完全不同的分析结构，而不是外壳不同、内核一样的两份报告。

---

## 二、五个预置Skill的方法论与对应输出结构

### 2.1 价格趋势分析 — 价格驱动四因子框架

**方法论**：将价格波动原因拆解为四类因子，逐一排查。

**输出结构**（本Skill专属的展示维度）：

| 维度 | 关注点 |
|---|---|
| 供给因子 | 产能变化、开工率、突发停产/事故 |
| 需求因子 | 下游行业景气度、替代品冲击 |
| 成本因子 | 原材料/能源/人工等上游输入成本传导 |
| 金融与政策因子 | 汇率波动、关税、库存投机行为、产业政策 |
| 展望 | 短中期价格预期及关键反转信号 |

### 2.2 供需关系分析 — 库存周期四阶段法

**方法论**：判断当前处于库存周期的哪个阶段，而非仅判断涨跌方向。

**输出结构**：

| 维度 | 关注点 |
|---|---|
| 当前阶段判断 | 主动补库存 / 被动补库存 / 主动去库存 / 被动去库存，及判断依据 |
| 供给端观察 | 产能利用率、库存水平变化趋势 |
| 需求端观察 | 下游采购节奏、订单变化 |
| 阶段转换信号 | 可能预示周期转换的关键指标或事件 |
| 展望 | 预计维持当前阶段的时间窗口 |

### 2.3 竞争格局分析 — 波特五力框架

**方法论**：从五种竞争力量评估该物料所在供应市场的结构性特征。

**输出结构**：

| 维度 | 关注点 |
|---|---|
| 供应商议价能力 | 供应商集中度、切换成本、原料稀缺性 |
| 买方（采购方）议价能力 | 采购方集中度、采购量对供应商的重要性 |
| 行业内竞争强度 | 现有供应商之间的价格竞争激烈程度 |
| 替代品威胁 | 是否存在可替代该物料的其他方案 |
| 新进入者威胁 | 该供应领域的进入壁垒高低 |

### 2.4 综合深度研究 — 五看三定框架（改造版）

**方法论**：源自华为战略管理框架"五看三定"中的战略洞察部分，改造为物料成本场景，是五个Skill中**唯一横向整合视角**的框架。

**输出结构**：

| 维度 | 关注点 |
|---|---|
| 看行业/趋势 | 宏观技术趋势、行业整体供需大势 |
| 看市场/客户 | 下游需求端，哪些产品/场景在拉动该物料需求 |
| 看竞争 | 供给端格局（可直接复用2.3节波特五力的分析结论，避免重复搜索与分析，见第三节） |
| 看自己 | 对我方采购的实际意义——议价地位、替代方案可行性、是否需要囤货 |
| 看机会 | 关键时机窗口，何种信号出现意味着适合锁价或适合观望 |

### 2.5 自定义 — 用户自定义框架模板脚手架

不预设固定方法论，但提供结构化的编写模板，要求用户自定义Skill时至少填写以下区块，确保自定义Skill与预置Skill在文件结构和数据展示上保持一致：

```markdown
---
name: [Skill名称]
description: [该Skill适用场景与核心视角]
---

## 分析维度
1. [维度名称] — [关注点说明]
2. [维度名称] — [关注点说明]
...（用户可自定义任意数量维度，建议3-6个）

## 证据要求
[对信息来源可信度、时效性的具体要求]

## 输出格式
[是否需要展望部分、是否需要给出建议动作类型等]
```

---

## 三、数据结构调整（重要：修正v5文档的固定枚举设计）

### 3.1 问题

v5 文档第2.2节中，`trend_insight_dimensions` 表的 `dimension_type` 字段被设计为固定的几个值（供需格局/价格驱动/卡点识别/政策地缘/展望），这实际上把所有Skill的输出锁死成了同一套结构，与本文档"每个Skill应有独特输出结构"的原则相悖。

### 3.2 修正方案

```sql
-- 修正：dimension_type 改为自由文本，不再是固定枚举
-- 具体取值由当次查询所使用的Skill决定（对照第二节各Skill的输出结构表）
CREATE TABLE trend_insight_dimensions (
    id INTEGER PRIMARY KEY,
    trend_snapshot_id INTEGER NOT NULL,
    dimension_type TEXT NOT NULL,       -- 自由文本，取值取决于所用Skill，如"供给因子""看机会""当前阶段判断"等
    dimension_order INTEGER,            -- 维度展示顺序，遵循该Skill定义的顺序
    content TEXT NOT NULL,
    evidence_strength TEXT NOT NULL,    -- '强'/'中'/'弱'/'未验证'
    source_title TEXT,
    source_url TEXT,
    FOREIGN KEY (trend_snapshot_id) REFERENCES trend_snapshots(id)
);

-- trend_snapshots 需新增字段，记录本次洞察使用的Skill，
-- 使历史记录可追溯"这份分析是用哪套方法论产出的"
ALTER TABLE trend_snapshots ADD COLUMN skill_used TEXT;  -- Skill的标识名，如'price-driver-four-factors'

-- Skill定义表：每个Skill对应一份独立文件及其输出结构定义
CREATE TABLE analysis_skills (
    id INTEGER PRIMARY KEY,
    skill_key TEXT NOT NULL UNIQUE,     -- 如 'price-trend-analysis'
    display_name TEXT NOT NULL,         -- 如 '价格趋势分析'
    file_path TEXT NOT NULL,            -- 对应的 .md 文件路径
    output_dimensions TEXT NOT NULL,    -- JSON数组，定义该Skill的输出维度及顺序，如 ["供给因子","需求因子","成本因子","金融与政策因子","展望"]
    is_preset BOOLEAN DEFAULT 1,        -- 1=预置Skill，0=用户自定义
    created_at DATETIME
);
```

### 3.3 界面展示要求

- 洞察结果页面顶部需明确标注**本次分析使用的Skill名称**（如"分析框架：波特五力·竞争格局分析"），让用户清楚知道当前看到的结构对应哪套方法论
- 不同Skill产出的结果，维度模块的数量、名称、排列顺序应严格按 `analysis_skills.output_dimensions` 中定义的结构渲染，不得所有Skill共用一套写死的前端模板
- 历史快照回溯时（v3文档第5.3节的时间轴回溯功能），需同时展示当时使用的是哪个Skill，避免用户误以为不同时间点的分析是同一套框架产出、可以直接横向对比结构

---

## 四、跨Skill复用（进阶设计，可选，不作为首期必须实现项）

第2.4节提到"综合深度研究"中的"看竞争"维度可直接复用"竞争格局分析"（波特五力）已产出的结果，这是一种跨Skill结果复用机制：当用户对同一物料先后运行过多个Skill时，"综合深度研究"在生成"看竞争"这部分内容时，可以优先检索该物料是否已有近期的波特五力分析快照，若有则引用其结论（并标注引用来源），若没有则重新生成。此设计可以减少重复搜索、提高综合报告的分析深度，但**实现复杂度较高，建议作为独立的后续迭代任务，不纳入首期开发范围**。

---

## 五、验收标准

- [ ] 五个预置Skill均已按第二节定义好各自独立的输出维度结构，并保存为独立`.md`文件
- [ ] `trend_insight_dimensions` 表的 `dimension_type` 字段改为自由文本，不再是固定枚举
- [ ] `trend_snapshots` 记录本次洞察所使用的Skill标识
- [ ] 洞察结果页面明确展示"本次使用的分析框架"名称
- [ ] 用不同Skill对同一物料发起洞察，返回结果的维度结构应明显不同（如波特五力返回五种竞争力维度，库存周期法返回四阶段判断），而非外观不同但内在结构一致
- [ ] 历史时间轴回溯时，能看到每次快照对应使用的Skill名称
- [ ] 自定义Skill遵循2.5节模板结构，可正常保存并在后续查询中按用户定义的维度产出结果
